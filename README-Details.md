
## Dashboard: SN-ALERTS

**Security use case examples:** Consolidates IDS/IPS alert data with geographic, protocol, and threat hunt panels to provide a comprehensive overview of detected threats and alert trends.

**Network use case examples:** Enables operations teams to baseline alert volumes and severity distribution over time, supporting detection coverage validation and sensor health monitoring.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Alert-ByExtraInfoType** | **Generic description:** Bar chart showing alert counts grouped by extra-info type fields attached to Suricata alert events. **Security use case examples:** Extra-info fields (e.g. **Network use case examples:** HTTP host, TLS SNI, DNS rrname) enrich alert context; this view shows which enrichment types dominate, aiding correlation with application-layer data. Helps operations teams understand the protocol context distribution of alerts, supporting decisions about which protocol parsers to enable or tune for better coverage. | `timestamp`, `app_proto` | event_type:alert |
| **SN-Alert-ByHttpMethod** | **Generic description:** Donut chart showing the distribution of HTTP methods (GET, POST, PUT, DELETE, etc.) observed in IDS alert events. **Security use case examples:** Unexpected HTTP methods in alert context — especially PUT, DELETE, or CONNECT — may indicate web application exploitation, proxy abuse, or data exfiltration attempts. **Network use case examples:** Tracks HTTP method usage patterns for web policy compliance, ensuring only approved methods are in use and flagging deviations from expected application behaviour. | `http.http_method` | event_type:alert |
| **SN-Alert-BySmtpHello** | **Generic description:** Donut chart showing the distribution of SMTP HELO/EHLO hostnames observed in alert-enriched email events. **Security use case examples:** Unusual or spoofed HELO strings are a common indicator of malicious mail infrastructure, spam campaigns, or phishing attacks using forged sender identities. **Network use case examples:** Validates mail relay policy by inventorying all HELO strings used by connecting mail servers, supporting SPF/DMARC compliance and mail gateway rule tuning. | `smtp.helo` | event_type:alert |
| **SN-Alert-ByTlsIssuerdn** | **Generic description:** Donut chart showing the distribution of TLS certificate issuer distinguished names (issuerDN) in alert-enriched TLS events. **Security use case examples:** Self-signed or unknown issuer DNs appearing in alert context strongly suggest malicious or adversary-controlled infrastructure using custom certificates for C2 or phishing. **Network use case examples:** Supports certificate authority (CA) policy enforcement by inventorying which CAs are issuing certificates to servers communicating with the network. | `tls.issuerdn` | event_type:alert |
| **SN-Alert-ByTlsSni** | **Generic description:** Donut chart showing the distribution of TLS Server Name Indication (SNI) values from alert-enriched TLS handshakes. **Security use case examples:** SNI values in alert context reveal the target domain or service of suspicious encrypted connections, enabling faster attribution and blocking of malicious domains. **Network use case examples:** Tracks SNI distribution across the network for monitoring outbound encrypted communications policy and validating domain whitelisting configurations. | `tls.sni` | event_type:alert |
| **SN-Alert-ByVLANIDTop20** | **Generic description:** Donut chart showing the top 20 VLAN IDs by IDS alert volume. **Security use case examples:** Highlights network segments generating disproportionate alert traffic, which may indicate compromised hosts, misconfigured services, or insufficient VLAN-level access controls. **Network use case examples:** Enables VLAN-level capacity and risk reporting, supporting network architecture reviews and decisions about VLAN consolidation or additional monitoring investment. | `vlan` | event_type:alert |
| **SN-Alert-Count** | **Generic description:** Single large-number metric display showing the total IDS/IPS alert count for the selected time range. **Security use case examples:** Provides an immediate high-level indicator of alert volume that can be compared against historical baselines to detect sudden threat surges or sensor failures. **Network use case examples:** Serves as a primary KPI for SOC dashboards and shift handover reports, giving operations teams an instant measure of security event activity. | — | event_type:alert |
| **SN-Alert-GeoMap** | **Generic description:** Geographic heatmap overlaying IDS/IPS alert source or destination IP geolocation data on a world map. **Security use case examples:** Visualises the global origin distribution of attack traffic, helping analysts identify geographically targeted campaigns or unusual source country patterns. **Network use case examples:** Supports geo-based network policy decisions such as country-level ACLs and validates that geo-blocking rules are correctly suppressing traffic from restricted regions. | `geoip.location` | event_type:alert |
| **SN-Alert-Proto** | **Generic description:** Donut chart showing the distribution of IDS/IPS alerts broken down by network protocol (TCP, UDP, ICMP, etc.). **Security use case examples:** Protocol distribution in alerts reveals which transport layers are most actively targeted, guiding firewall hardening and IDS rule prioritisation efforts. **Network use case examples:** Provides a protocol-level traffic profiling view for network policy compliance, confirming that unexpected protocols are flagged and expected service protocols remain dominant. | `proto` | event_type:alert |
| **SN-Alert-Top20DstIP** | **Generic description:** Donut chart ranking the top 20 destination IP addresses receiving the most IDS/IPS alerts. **Security use case examples:** Highlights the most targeted internal assets, enabling rapid identification of systems under active attack or reconnaissance and prioritising protective actions. **Network use case examples:** Supports asset exposure analysis by revealing which servers attract the most attack traffic, informing network segmentation and prioritised patch management. | `dest_ip` | event_type:alert |
| **SN-Alert-Top20DstPorts** | **Generic description:** Donut chart showing the top 20 destination ports by IDS alert count. **Security use case examples:** Identifies the services most actively targeted by attacks or scans, guiding firewall rule hardening and highlighting exposed service ports requiring immediate attention. **Network use case examples:** Supports network port policy enforcement by showing which destination ports generate the most alert traffic, validating that high-risk ports are restricted as intended. | `dest_port` | event_type:alert |
| **SN-Alert-Top20SrcIP** | **Generic description:** Donut chart ranking the top 20 source IP addresses generating the most IDS/IPS alerts. **Security use case examples:** Identifies the most prolific attack sources — whether external threat actors, compromised internal hosts, or scanners — enabling targeted blocking and incident triage. **Network use case examples:** Provides a source IP reputation baseline for network monitoring, supporting automated threat intelligence enrichment and block-list management workflows. | `src_ip` | event_type:alert |
| **SN-Alert-Top20SrcPorts** | **Generic description:** Donut chart showing the top 20 source ports appearing in IDS alert traffic. **Security use case examples:** Unusual source port distributions (e.g. high ephemeral-port attacks or low-port spoofing) can indicate specific exploitation techniques or evasion attempts. **Network use case examples:** Validates network ACL effectiveness by confirming that expected source-port restrictions are generating alerts when bypassed. | `src_port` | event_type:alert |
| **SN-ThreatHunt-ALERTS-MutlipleUniqueAlertOnDestIP** | **Generic description:** Data table ranking destination IPs by the count of unique IDS signatures triggered against them, surfacing heavily targeted hosts. **Security use case examples:** Destination hosts attracting many different alert signatures may be under active exploitation, serving as honeypot-like targets, or hosting vulnerable services being probed. **Network use case examples:** Supports network asset prioritisation by identifying servers receiving disproportionate scanning or attack traffic, informing patch management and firewall rule reviews. | `dest_ip`, `alert.signature` | event_type:alert |
| **SN-ThreatHunt-ALERTS-MutlipleUniqueAlertOnSrcIP** | **Generic description:** Data table ranking source IPs by the count of unique IDS alert signatures they triggered, highlighting hosts generating diverse alert types. **Security use case examples:** A host triggering many distinct signatures simultaneously often indicates active exploitation, malware multi-stage activity, or automated scanning tools — a high-priority triage signal. **Network use case examples:** Establishes a per-IP signature diversity baseline, enabling operations teams to set thresholds for automated escalation and prioritise endpoint investigation queues. | `src_ip`, `alert.signature` | event_type:alert |
| **SN-ThreatHunt-ALERTS-Top100Signatures** | **Generic description:** Data table listing the top 100 IDS/IPS signatures (rule names) by trigger count, ranked by frequency. **Security use case examples:** Identifies the most active detection rules in the environment — the top signatures often reveal the primary attack vectors or the noisiest misconfigured rules requiring tuning. **Network use case examples:** Supports rule management workflows by providing quantitative data for suppression, threshold tuning, or rule prioritisation during periodic IDS maintenance cycles. | `alert.signature` | event_type:alert |
| **SN-ALERT-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-ALERTS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALERT events | — |

## Dashboard: SN-ALL

**Security use case examples:** High-level multi-protocol overview dashboard combining alert metrics, protocol event timelines, and traffic distribution to provide a single-pane-of-glass operational view.

**Network use case examples:** Provides operations teams with a unified view of multi-protocol traffic volumes to baseline overall network activity and monitor the health of all monitored segments.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Alert-ByExtraInfoType** | **Generic description:** Bar chart showing alert counts grouped by extra-info type fields attached to Suricata alert events. **Security use case examples:** Extra-info fields (e.g. **Network use case examples:** HTTP host, TLS SNI, DNS rrname) enrich alert context; this view shows which enrichment types dominate, aiding correlation with application-layer data. Helps operations teams understand the protocol context distribution of alerts, supporting decisions about which protocol parsers to enable or tune for better coverage. | `timestamp`, `app_proto` | event_type:alert |
| **SN-Alert-Count** | **Generic description:** Single large-number metric display showing the total IDS/IPS alert count for the selected time range. **Security use case examples:** Provides an immediate high-level indicator of alert volume that can be compared against historical baselines to detect sudden threat surges or sensor failures. **Network use case examples:** Serves as a primary KPI for SOC dashboards and shift handover reports, giving operations teams an instant measure of security event activity. | — | event_type:alert |
| **SN-DNS-DnsOverTime** | **Generic description:** Multi-series chart showing DNS query and response event counts over time. **Security use case examples:** DNS traffic spikes or sustained high volumes can indicate DGA-based malware activity, DNS amplification attacks, or DNS tunnelling exfiltration sessions. **Network use case examples:** Provides a DNS infrastructure capacity baseline, supporting DNS resolver sizing decisions and detection of query volume anomalies relative to business hours. | `timestamp` | event_type:dns |
| **SN-EventTypeOverTimeAll** | **Generic description:** Multi-line or stacked bar chart showing all Suricata event types plotted over time, providing a complete event volume picture. **Security use case examples:** A comprehensive event type timeline enables cross-protocol correlation — a DNS spike followed by HTTP activity followed by TLS connections may indicate a multi-stage attack. **Network use case examples:** Provides a holistic traffic baseline across all monitored protocols, supporting scheduled capacity reviews and identifying protocol volume trends for infrastructure planning. | `timestamp`, `event_type` | event_type:all |
| **SN-FILE-EventsOverTime** | **Generic description:** Multi-series line chart showing file transaction event counts over time, broken down by file type or protocol. **Security use case examples:** Sudden spikes in file transfer volumes — especially executables or archives — may indicate malware distribution campaigns, staged exfiltration, or bulk download activity. **Network use case examples:** Provides a file transfer traffic baseline for DLP capacity planning and validates that file transfer volumes align with expected business activity patterns. | List of FILEINFO events | event_type:fileinfo |
| **SN-Proto-app_proto** | **Generic description:** Donut chart showing the distribution of all observed application protocols (app_proto field) across all event types. **Security use case examples:** Identifies unusual or unexpected protocols in the environment — such as raw MQTT, Kerberos over unexpected ports, or unknown protocols — that warrant investigation. **Network use case examples:** Provides a complete application-layer protocol inventory for network segmentation, policy validation, and compliance reporting across the monitored environment. | `proto`, `app_proto` | event_type:all |
| **SN-SMTP-SmtpOverTime** | **Generic description:** Line chart showing SMTP event volume over time. **Security use case examples:** SMTP volume spikes outside business hours, or from unexpected source ranges, may indicate compromised email accounts being used for spam, phishing campaigns, or mail bombing. **Network use case examples:** Provides an email traffic capacity baseline for mail server sizing and detecting seasonal volume trends for infrastructure planning. | `timestamp` | event_type:smtp |
| **SN-SSH-EventsOverTime** | **Generic description:** Line chart showing SSH connection event volume over time. **Security use case examples:** SSH volume spikes, especially from external IPs or during off-hours, are a strong indicator of brute-force campaigns, credential stuffing, or lateral movement activity. **Network use case examples:** Provides an SSH traffic baseline for capacity planning and validates SSH gateway utilisation patterns for infrastructure sizing decisions. | List of SSH events | event_type:ssh |
| **SN-ThreatHunt-ALERTS-Top100Signatures** | **Generic description:** Data table listing the top 100 IDS/IPS signatures (rule names) by trigger count, ranked by frequency. **Security use case examples:** Identifies the most active detection rules in the environment — the top signatures often reveal the primary attack vectors or the noisiest misconfigured rules requiring tuning. **Network use case examples:** Supports rule management workflows by providing quantitative data for suppression, threshold tuning, or rule prioritisation during periodic IDS maintenance cycles. | `alert.signature` | event_type:alert |
| **SN-TLS-EventsOverTime** | **Generic description:** Line chart showing TLS connection event volume over time. **Security use case examples:** TLS volume spikes or drops can indicate TLS inspection bypass attempts, encrypted malware distribution campaigns, or TLS service outages. **Network use case examples:** Provides a TLS traffic volume baseline for capacity planning and monitoring TLS inspection infrastructure utilisation across time periods. | List of TLS events | event_type:tls |
| **SN-ALL-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-ALL events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALL events | — |

## Dashboard: SN-ANOMALY

**Security use case examples:** Provides visibility into Suricata protocol anomaly events to detect evasion attempts, malformed packet attacks, and protocol compliance violations.

**Network use case examples:** Supports network health monitoring by tracking decoder anomaly rates as an indicator of traffic quality, helping prioritise Suricata parser tuning and sensor calibration.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-ANOMALY-ByVlan** | **Generic description:** Donut chart showing the distribution of anomaly events by VLAN ID. **Security use case examples:** VLANs with disproportionate anomaly counts may contain misconfigured devices, evasion traffic, or compromised hosts generating protocol-violating packets. **Network use case examples:** Supports network segmentation health monitoring by identifying which VLANs produce anomaly events, enabling targeted investigation of segment-level protocol issues. | `anomaly.vlan` | event_type:anomaly |
| **SN-ANOMALY-Code** | **Generic description:** Donut chart showing the distribution of specific anomaly code values reported by Suricata. **Security use case examples:** Specific anomaly codes correlate with known evasion techniques or protocol violations; repeated appearance may indicate targeted attempts to exploit Suricata parser weaknesses. **Network use case examples:** Provides detailed anomaly code inventory for Suricata decoder tuning and false-positive reduction in high-anomaly environments. | `anomaly.code` | event_type:anomaly |
| **SN-ANOMALY-EventsOverTimeByAppProto** | **Generic description:** Stacked bar chart showing anomaly event counts over time, broken down by application-layer protocol. **Security use case examples:** Protocol-specific anomaly spikes — e.g. sudden TLS or HTTP anomaly surges — may indicate targeted evasion attempts, protocol fuzzing attacks, or Suricata parser bugs. **Network use case examples:** Tracks anomaly distribution across protocols for capacity planning of Suricata's application-layer decoders and helps prioritise parser tuning efforts. | List of ANOMALY events | event_type:anomaly |
| **SN-ANOMALY-EventType** | **Generic description:** Donut chart showing the distribution of Suricata anomaly event types (decoder, stream, applayer). **Security use case examples:** The breakdown between decoder, stream, and application-layer anomaly types reveals whether anomalies are low-level protocol violations or higher-level application parsing failures. **Network use case examples:** Provides anomaly event type taxonomy for sensor health reporting and prioritises Suricata configuration tuning efforts for the highest-impact anomaly categories. | `anomaly.event` | event_type:anomaly |
| **SN-ANOMALY-Layer** | **Generic description:** Donut chart showing the distribution of network layers (L2, L3, L4, L7) where Suricata detected anomalies. **Security use case examples:** Layer-specific anomaly concentrations reveal which network stack levels are being abused; L4 anomalies often indicate evasion, while L7 anomalies suggest protocol exploitation. **Network use case examples:** Supports sensor decoder health monitoring by identifying which protocol layers generate the most anomaly events, guiding decoder configuration and tuning priorities. | `anomaly.layer` | event_type:anomaly |
| **SN-ANOMALY-Top100-DestIP** | **Generic description:** Data table ranking the top 100 destination IPs targeted by anomaly-generating connections. **Security use case examples:** Destinations consistently receiving anomalous traffic may be targeted by evasion attempts, protocol fuzzing, or malformed packet injection to exploit application vulnerabilities. **Network use case examples:** Supports asset protection prioritisation by identifying which servers receive the most anomalous traffic for targeted hardening and monitoring. | `dest_ip` | event_type:anomaly |
| **SN-ANOMALY-Top100-DestPort** | **Generic description:** Data table ranking the top 100 destination ports in anomaly-generating traffic. **Security use case examples:** Services on specific destination ports consistently receiving anomalous traffic may be targeted by exploit probes or experiencing compatibility issues with certain client implementations. **Network use case examples:** Supports service health monitoring by correlating anomaly events with destination ports, enabling port-specific protocol tuning and firewall rule adjustments. | `dest_port` | event_type:anomaly |
| **SN-ANOMALY-Top100-SrcIP** | **Generic description:** Data table ranking the top 100 source IPs generating anomaly events. **Security use case examples:** Hosts consistently generating protocol anomalies may be running scanning tools, malware, or misconfigured applications that produce malformed or non-standard protocol messages. **Network use case examples:** Identifies problematic source hosts for network troubleshooting and enables targeted investigation of endpoints generating disproportionate anomaly traffic. | `src_ip` | event_type:anomaly |
| **SN-ANOMALY-Top100-SrcPort** | **Generic description:** Data table ranking the top 100 source ports in anomaly events. **Security use case examples:** Source ports associated with anomaly events can help attribute the anomaly-generating tool or identify specific application configurations generating malformed traffic. **Network use case examples:** Supports network protocol analysis by mapping which source port ranges generate the most anomaly events, informing firewall and application configuration reviews. | `src_port` | event_type:anomaly |
| **SN-ANOMALY-TotalCount** | **Generic description:** Single large-number metric showing the total count of Suricata anomaly events in the selected time range. **Security use case examples:** High anomaly counts may indicate malformed packet injection, evasion attempts using protocol violations, or underlying sensor decode errors requiring investigation. **Network use case examples:** Provides an anomaly event volume KPI for sensor health monitoring and baseline comparison across time periods. | — | event_type:anomaly |
| **SN-ANOMALY-Type** | **Generic description:** Donut chart showing the distribution of Suricata anomaly types (decode, stream, applayer, etc.). **Security use case examples:** High rates of specific anomaly types (e.g. stream anomalies) may indicate TCP-level evasion attempts or fragmentation attacks targeting the Suricata stream engine. **Network use case examples:** Provides an anomaly taxonomy for sensor health monitoring, enabling operations teams to understand whether anomalies are protocol-level or application-layer in origin. | `anomaly.type` | event_type:anomaly |
| **SN-ANOMALY-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-ANOMALY events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ANOMALY events | — |

## Dashboard: SN-DHCP

**Security use case examples:** Provides DHCP protocol monitoring to detect rogue DHCP servers, DHCP starvation attacks, and addressing policy violations.

**Network use case examples:** Supports address management by tracking DHCP lease rates, pool utilisation, and client device counts to aid IP address capacity planning and infrastructure growth decisions.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-DHCP-Detailed-Type** | **Generic description:** Donut chart showing the distribution of DHCP Detailed-Type data. **Security use case examples:** Analysis of DHCP detailed-type distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DHCP protocol monitoring for capacity planning and validates that detailed-type patterns align with expected configurations. | `dhcp.dhcp_type` | event_type:dhcp |
| **SN-DHCP-EventsOverTime** | **Generic description:** Bar chart showing DHCP event volume over time. **Security use case examples:** Tracking DHCP traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides DHCP traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of DHCP events | event_type:dhcp |
| **SN-DHCP-Releays** | **Generic description:** Donut chart showing the distribution of DHCP Releays data. **Security use case examples:** Analysis of DHCP releays distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DHCP protocol monitoring for capacity planning and validates that releays patterns align with expected configurations. | `dhcp.relay_ip` | event_type:dhcp |
| **SN-DHCP-Routers-Servers** | **Generic description:** Donut chart showing the distribution of DHCP Routers-Servers data. **Security use case examples:** Analysis of DHCP routers-servers distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DHCP protocol monitoring for capacity planning and validates that routers-servers patterns align with expected configurations. | `dhcp.routers` | event_type:dhcp |
| **SN-DHCP-SubnetMasks-Served** | **Generic description:** Donut chart showing the distribution of DHCP SubnetMasks-Served data. **Security use case examples:** Analysis of DHCP subnetmasks-served distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DHCP protocol monitoring for capacity planning and validates that subnetmasks-served patterns align with expected configurations. | `dhcp.subnet_mask` | event_type:dhcp |
| **SN-DHCP-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for DHCP connections. **Security use case examples:** Identifying the most active DHCP destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that DHCP traffic uses only expected destination IPs. | `dest_ip` | event_type:dhcp |
| **SN-DHCP-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for DHCP connections. **Security use case examples:** Identifying the most active DHCP destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that DHCP traffic uses only expected destination ports. | `dest_port` | event_type:dhcp |
| **SN-DHCP-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for DHCP connections. **Security use case examples:** Identifying the most active DHCP source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that DHCP traffic uses only expected source IPs. | `src_ip` | event_type:dhcp |
| **SN-DHCP-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for DHCP connections. **Security use case examples:** Identifying the most active DHCP source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that DHCP traffic uses only expected source ports. | `src_port` | event_type:dhcp |
| **SN-DHCP-Total** | **Generic description:** Single metric display showing the total count of DHCP events. **Security use case examples:** Provides an at-a-glance DHCP event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary DHCP traffic KPI for operational monitoring and capacity planning. | — | event_type:dhcp |
| **SN-DHCP-Type** | **Generic description:** Donut chart showing the distribution of DHCP Type data. **Security use case examples:** Analysis of DHCP type distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DHCP protocol monitoring for capacity planning and validates that type patterns align with expected configurations. | `dhcp.type` | event_type:dhcp |
| **SN-DHCP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-DHCP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of DHCP events | — |

## Dashboard: SN-DNP3

**Security use case examples:** Monitors DNP3 SCADA/ICS protocol communications to detect unauthorised OT device access, command injection, and industrial network policy violations.

**Network use case examples:** Enables OT network operators to baseline DNP3 device communication patterns, monitor session counts, and plan bandwidth allocation for industrial control system traffic.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-DNP3-ByDestIP** | **Generic description:** Data table showing the distribution of DNP3 ByDestIP data. **Security use case examples:** Analysis of DNP3 destip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that destip patterns align with expected configurations. | `dest_ip` | event_type:dnp3 |
| **SN-DNP3-ByDestPort** | **Generic description:** Data table showing the distribution of DNP3 ByDestPort data. **Security use case examples:** Analysis of DNP3 destport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that destport patterns align with expected configurations. | `dest_port` | event_type:dnp3 |
| **SN-DNP3-ByDst** | **Generic description:** Donut chart showing the distribution of DNP3 ByDst data. **Security use case examples:** Analysis of DNP3 dst distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that dst patterns align with expected configurations. | `dnp3.dst` | event_type:dnp3 |
| **SN-DNP3-ByIndicators** | **Generic description:** Donut chart showing the distribution of DNP3 ByIndicators data. **Security use case examples:** Analysis of DNP3 indicators distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that indicators patterns align with expected configurations. | `dnp3.iin.indicators` | event_type:dnp3 |
| **SN-DNP3-BySrc** | **Generic description:** Donut chart showing the distribution of DNP3 BySrc data. **Security use case examples:** Analysis of DNP3 src distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that src patterns align with expected configurations. | `dnp3.src` | event_type:dnp3 |
| **SN-DNP3-BySrcIP** | **Generic description:** Data table showing the distribution of DNP3 BySrcIP data. **Security use case examples:** Analysis of DNP3 srcip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that srcip patterns align with expected configurations. | `src_ip` | event_type:dnp3 |
| **SN-DNP3-BySrcPort** | **Generic description:** Data table showing the distribution of DNP3 BySrcPort data. **Security use case examples:** Analysis of DNP3 srcport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that srcport patterns align with expected configurations. | `src_port` | event_type:dnp3 |
| **SN-DNP3-ByType** | **Generic description:** Donut chart showing the distribution of DNP3 ByType data. **Security use case examples:** Analysis of DNP3 type distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports DNP3 protocol monitoring for capacity planning and validates that type patterns align with expected configurations. | `dnp3.type` | event_type:dnp3 |
| **SN-DNP3-Count** | **Generic description:** Single metric display showing the total count of DNP3 events. **Security use case examples:** Provides an at-a-glance DNP3 event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary DNP3 traffic KPI for operational monitoring and capacity planning. | — | event_type:dnp3 |
| **SN-DNP3-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-DNP3 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of DNP3 events | — |

## Dashboard: SN-DNS

**Security use case examples:** Provides detailed visibility into DNS protocol activity to detect DGA malware, DNS tunnelling, NXDOMAIN floods, and DNS covert channel exfiltration.

**Network use case examples:** Supports DNS infrastructure monitoring by tracking query volumes, response codes, and resolver load to aid capacity planning, uptime management, and service performance baselining.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-DNS-ByProto** | **Generic description:** Donut chart showing the distribution of DNS transport protocol usage (UDP vs TCP) across DNS events. **Security use case examples:** Elevated DNS-over-TCP ratios may indicate large DNS responses (DNSSEC, zone transfers), TCP-based DNS tunnelling tools, or DNS resolver configuration issues. **Network use case examples:** Tracks DNS transport protocol distribution for infrastructure planning and validates that DNS-over-TCP is only used for legitimate large-response scenarios. | `proto` | event_type:dns |
| **SN-DNS-ByTtl** | **Generic description:** Bar or line chart showing the distribution of DNS record TTL values across DNS responses. **Security use case examples:** Very short TTLs (0–10 seconds) are associated with fast-flux DNS used by botnet C2 infrastructure and CDN-based malware delivery to avoid blacklisting. **Network use case examples:** Tracks TTL distribution trends for DNS policy compliance, supporting monitoring of legitimate CDN usage and detection of fast-flux infrastructure in the environment. | `dns.answers.ttl` | event_type:dns |
| **SN-DNS-DnsOverTime** | **Generic description:** Multi-series chart showing DNS query and response event counts over time. **Security use case examples:** DNS traffic spikes or sustained high volumes can indicate DGA-based malware activity, DNS amplification attacks, or DNS tunnelling exfiltration sessions. **Network use case examples:** Provides a DNS infrastructure capacity baseline, supporting DNS resolver sizing decisions and detection of query volume anomalies relative to business hours. | `timestamp` | event_type:dns |
| **SN-DNS-GeoIP** | **Generic description:** Geographic heatmap overlaying DNS query and response traffic on a world map by source/destination geolocation. **Security use case examples:** Unusual geographic patterns in DNS traffic — queries to foreign resolvers or responses from unexpected countries — may indicate DNS hijacking or resolver policy bypasses. **Network use case examples:** Supports network DNS routing policy validation by confirming that DNS traffic flows to expected resolver locations and identifying unauthorised use of external resolvers. | `geoip.location` | event_type:dns |
| **SN-DNS-Rcode** | **Generic description:** Donut chart showing the distribution of DNS response codes (NOERROR, NXDOMAIN, SERVFAIL, REFUSED, etc.). **Security use case examples:** High NXDOMAIN ratios indicate DGA activity or misconfigured clients; SERVFAIL spikes may reveal DNS infrastructure problems or targeted DNS denial-of-service. **Network use case examples:** Supports DNS service health monitoring by tracking response code distributions and detecting resolver degradation or configuration errors before user impact. | `dns.rcode` | event_type:dns |
| **SN-DNS-Rdata** | **Generic description:** Donut chart showing the most frequently resolved DNS response data values (IP addresses or CNAME targets). **Security use case examples:** Frequently resolved IP addresses may reveal CDN infrastructure, C2 beacons resolving repeatedly to the same address, or DGA clusters sharing a common IP. **Network use case examples:** Enables DNS response inventory for network topology mapping and tracks CDN or cloud provider IP usage to support capacity and connectivity planning. | `dns.answers.rdata` | event_type:dns |
| **SN-DNS-Rrname** | **Generic description:** Donut chart showing the top resolved domain names (rrname/query name field) by query frequency. **Security use case examples:** High-frequency queries to unusual, randomised, or encoded domain names are strong indicators of DGA activity, DNS tunnelling, or beaconing behaviour. **Network use case examples:** Provides a DNS query inventory for network access policy validation, confirming that internal hosts are only resolving expected domains and not bypassing DNS controls. | `dns.rrname` | event_type:dns |
| **SN-DNS-Rrtype** | **Generic description:** Donut chart showing the distribution of DNS record types (A, AAAA, CNAME, MX, TXT, PTR, NULL, etc.) in DNS traffic. **Security use case examples:** Unusual record type distributions — high TXT, NULL, or ANY queries — are associated with DNS tunnelling tools and data exfiltration over DNS. **Network use case examples:** Supports DNS policy monitoring by providing a record-type baseline, enabling detection of unexpected record types that may require resolver policy restrictions. | `dns.rrtype` | event_type:dns |
| **SN-DNS-Top20DestIP** | **Generic description:** Data table ranking the top 20 destination IP addresses receiving DNS queries (typically DNS resolvers). **Security use case examples:** Unexpected DNS resolver IPs appearing in top destinations may indicate unauthorised resolver usage, DNS hijacking, or internal hosts bypassing DNS policy. **Network use case examples:** Maps DNS resolver usage across the network, supporting DNS infrastructure capacity planning and confirming that internal DNS policies are being followed. | `dest_ip` | event_type:dns |
| **SN-DNS-Top20DestPort** | **Generic description:** Data table ranking the top 20 destination ports in DNS traffic. **Security use case examples:** DNS traffic on ports other than 53 (or 853 for DoT) may indicate DNS-over-non-standard-port tunnelling tools or misconfigured DNS clients. **Network use case examples:** Validates DNS port policy compliance, confirming that DNS traffic uses only approved ports and flagging any DNS resolver configurations deviating from policy. | `dest_port` | event_type:dns |
| **SN-DNS-Top20SrcIP** | **Generic description:** Data table ranking the top 20 source IP addresses generating DNS queries. **Security use case examples:** A single host generating disproportionate DNS query volume may be running automated scanning, DGA malware, or a misconfigured application generating DNS noise. **Network use case examples:** Identifies the most active DNS clients for capacity planning and enables per-client DNS usage auditing to validate resolver access policies. | `src_ip` | event_type:dns |
| **SN-DNS-Top20SrcPort** | **Generic description:** Data table ranking the top 20 source ports used in DNS query traffic. **Security use case examples:** DNS queries consistently originating from a fixed source port may indicate source port predictability vulnerabilities or tunnelling tools. **Network use case examples:** Validates DNS source port randomisation practices for DNS cache poisoning resistance assessment and resolver security posture monitoring. | `src_port` | event_type:dns |
| **SN-DNS-Total** | **Generic description:** Single large-number metric showing the total count of DNS events in the selected time range. **Security use case examples:** Provides an at-a-glance DNS traffic volume indicator for detecting sudden drops (sensor/resolver failure) or surges (DGA activity, amplification attacks). **Network use case examples:** Serves as a primary DNS capacity KPI for infrastructure planning and shift handover dashboards. | — | event_type:dns |
| **SN-DNS-Type** | **Generic description:** Donut chart showing the distribution of DNS event types (query vs response) across captured DNS traffic. **Security use case examples:** Asymmetry between query and response counts can indicate DNS reflection attacks, dropped responses, or resolver failures affecting DNS availability. **Network use case examples:** Provides a query/response ratio metric for DNS service health monitoring and validates that DNS resolver responses are being captured correctly by the sensor. | `dns.type` | event_type:dns |
| **SN-DNS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-DNS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of DNS events | — |

## Dashboard: SN-FILE-Transactions

**Security use case examples:** Monitors network file transfer activity across protocols to detect malware delivery, data exfiltration, and policy-violating file transfer behaviour.

**Network use case examples:** Enables tracking of file transfer volumes by protocol and type to support DLP capacity planning, storage bandwidth baselining, and data transfer policy auditing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-FILE-ByAppProto** | **Generic description:** Donut chart showing the distribution of file transactions by application-layer protocol (HTTP, SMTP, FTP, SMB, etc.). **Security use case examples:** Unexpected protocols carrying file transfers may indicate covert exfiltration channels or protocol misuse for staging malware or data theft. **Network use case examples:** Provides a file transfer protocol inventory for data governance and DLP policy development, showing which protocols carry the most file transfer activity. | `app_proto` | event_type:fileinfo |
| **SN-FILE-ByHTTPByHostnameServed** | **Generic description:** Donut chart showing the distribution of HTTP hostnames serving file downloads, ranked by file transfer count. **Security use case examples:** Unknown or suspicious hostnames serving files may indicate malware distribution infrastructure, compromised CDN nodes, or phishing kit hosting. **Network use case examples:** Supports CDN auditing and hostname-based filtering policy validation by inventorying all HTTP hosts participating in file transfers. | `http.hostname` | event_type:fileinfo |
| **SN-FILE-ByTypeOverTime** | **Generic description:** Stacked line chart showing file transaction counts broken down by file type (MIME type or extension) over time. **Security use case examples:** Sudden spikes in executable (.exe, .dll, .ps1) or document (.pdf, .doc, .xls) transfers may indicate malware distribution campaigns or targeted file-based attack activity. **Network use case examples:** Provides a file type trend baseline for DLP and content filtering policy reviews, enabling operations teams to track which file categories dominate transfer activity. | `timestamp`, `fileinfo.magic` | event_type:fileinfo |
| **SN-FILE-GeoIPPDFAndExecutables** | **Generic description:** Geographic heatmap showing the geolocation of hosts involved in PDF and executable file transfers captured in network traffic. **Security use case examples:** Unexpected foreign sources delivering executable files or PDFs can indicate malware distribution, drive-by downloads, or targeted spear-phishing file delivery. **Network use case examples:** Supports data exfiltration policy monitoring by providing geographic context for file transfer origins and destinations, validating geo-based DLP restrictions. | `geoip.location` | event_type:fileinfo |
| **SN-FILE-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for FILE connections. **Security use case examples:** Identifying the most active FILE destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that FILE traffic uses only expected destination IPs. | `dest_ip` | event_type:fileinfo |
| **SN-FILE-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for FILE connections. **Security use case examples:** Identifying the most active FILE destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that FILE traffic uses only expected destination ports. | `dest_port` | event_type:fileinfo |
| **SN-FILE-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for FILE connections. **Security use case examples:** Identifying the most active FILE source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that FILE traffic uses only expected source IPs. | `src_ip` | event_type:fileinfo |
| **SN-FILE-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for FILE connections. **Security use case examples:** Identifying the most active FILE source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that FILE traffic uses only expected source ports. | `src_port` | event_type:fileinfo |
| **SN-FILE-Total** | **Generic description:** Single large-number metric displaying the total count of file transaction events in the selected time range. **Security use case examples:** Provides an at-a-glance file transfer volume indicator for detecting unexpected surges (data exfiltration campaigns) or anomalous patterns in file distribution activity. **Network use case examples:** Serves as a primary file transfer KPI for DLP capacity planning and data governance reporting dashboards. | — | event_type:fileinfo |
| **SN-FILE-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-FILE-Transactions events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of FILE events | — |

## Dashboard: SN-FLOW

**Security use case examples:** Visualises network flow metadata including protocol distribution, unique IP pair counts, and flow duration to support network baselining and capacity planning.

**Network use case examples:** Supports network capacity planning and topology visibility by providing a protocol-level view of session counts, connection durations, and active endpoint cardinality across the monitored network.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** Stacked bar chart showing event counts broken down by application-layer protocol (app_proto) over time. **Security use case examples:** Unexpected protocols appearing in flow data — or sudden drops in expected protocols — can signal service failures, protocol tunnelling, or exfiltration over non-standard channels. **Network use case examples:** Provides a comprehensive protocol inventory for capacity planning, enabling operations teams to track protocol adoption trends and validate that decommissioned protocols have been removed. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow unique count of src and dst IP** | **Generic description:** Line chart tracking the unique count of source and destination IP address pairs observed in network flows over time. **Security use case examples:** Rapid increases in unique IP pair counts indicate scanning, bot activity, or DDoS attack preparation; a sudden drop may signal sensor failure or network isolation. **Network use case examples:** Establishes the normal communicating host population baseline, supporting capacity planning for session tracking infrastructure and anomaly detection thresholds. | `dest_ip`, `timestamp`, `src_ip` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** Dual-axis line chart tracking mean flow duration (age) and total flow count simultaneously over time. **Security use case examples:** Unusually long-lived flows may indicate persistent C2 connections, data exfiltration sessions, or abandoned connections; short mean ages with high counts can signal scanning. **Network use case examples:** Provides flow lifecycle metrics for network capacity planning, helping operations teams understand connection duration patterns and session table sizing requirements. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-FLOW events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of FLOW events | — |

## Dashboard: SN-FLOW-BITTORRENT

**Security use case examples:** Flow-level visibility dashboard for BitTorrent traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse.

**Network use case examples:** Enables network operations teams to quantify and trend P2P traffic volumes for bandwidth policy enforcement and capacity impact assessment.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including BITTORRENT flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports P2P protocol bandwidth consumption and policy enforcement across the network within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks BitTorrent flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse. **Network use case examples:** Helps identify top-volume sessions for BITTORRENT traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in BitTorrent flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active BITTORRENT communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for BitTorrent sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse. **Network use case examples:** Supports capacity planning by showing BITTORRENT session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all BitTorrent flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate policy-violating P2P file sharing, potential malware distribution via torrents, and bandwidth abuse. **Network use case examples:** Provides an operational log view for BITTORRENT flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-DCERPC

**Security use case examples:** Flow-level visibility dashboard for DCERPC (Distributed Computing Environment RPC) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC.

**Network use case examples:** Supports Windows infrastructure capacity planning by tracking DCERPC service usage, endpoint connection counts, and RPC call volumes over time.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including DCERPC flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports DCERPC service usage counts, endpoint interface access patterns, and Windows RPC traffic baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks DCERPC (Distributed Computing Environment RPC) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC. **Network use case examples:** Helps identify top-volume sessions for DCERPC traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in DCERPC (Distributed Computing Environment RPC) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active DCERPC communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for DCERPC (Distributed Computing Environment RPC) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC. **Network use case examples:** Supports capacity planning by showing DCERPC session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all DCERPC (Distributed Computing Environment RPC) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate lateral movement, WMI and SMB-based exploitation, and remote service enumeration over DCERPC. **Network use case examples:** Provides an operational log view for DCERPC flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-DHCP

**Security use case examples:** Flow-level visibility dashboard for DHCP (Dynamic Host Configuration Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection.

**Network use case examples:** Enables DHCP capacity management by monitoring address lease session counts, client cardinality, and address renewal traffic volumes for infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including DHCP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports address pool utilisation and lease renewal rate capacity planning within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks DHCP (Dynamic Host Configuration Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection. **Network use case examples:** Helps identify top-volume sessions for DHCP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in DHCP (Dynamic Host Configuration Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active DHCP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for DHCP (Dynamic Host Configuration Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection. **Network use case examples:** Supports capacity planning by showing DHCP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all DHCP (Dynamic Host Configuration Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate rogue DHCP server attacks, IP exhaustion attacks, and unauthorised device detection. **Network use case examples:** Provides an operational log view for DHCP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-DNP3

**Security use case examples:** Flow-level visibility dashboard for DNP3 (Distributed Network Protocol 3) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance.

**Network use case examples:** Supports OT network capacity planning by monitoring DNP3 session counts, communication rates, and device polling traffic volumes across industrial segments.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including DNP3 flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports DNP3 master-to-slave session counts and polling rate baselines in OT environments within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks DNP3 (Distributed Network Protocol 3) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance. **Network use case examples:** Helps identify top-volume sessions for DNP3 traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in DNP3 (Distributed Network Protocol 3) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active DNP3 communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for DNP3 (Distributed Network Protocol 3) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance. **Network use case examples:** Supports capacity planning by showing DNP3 session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all DNP3 (Distributed Network Protocol 3) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate ICS/SCADA protocol abuse, unauthorised command injection, and OT network reconnaissance. **Network use case examples:** Provides an operational log view for DNP3 flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-DNS

**Security use case examples:** Flow-level visibility dashboard for DNS (Domain Name System) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing.

**Network use case examples:** Enables DNS infrastructure capacity planning by tracking resolver session counts, query rates, and unique client-to-server pair cardinality for service dimensioning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including DNS flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports DNS query volume baselines and resolver performance monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks DNS (Domain Name System) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing. **Network use case examples:** Helps identify top-volume sessions for DNS traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in DNS (Domain Name System) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active DNS communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for DNS (Domain Name System) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing. **Network use case examples:** Supports capacity planning by showing DNS session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all DNS (Domain Name System) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate DNS tunneling, data exfiltration via DNS, DGA activity, and C2 beaconing. **Network use case examples:** Provides an operational log view for DNS flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-ENIP

**Security use case examples:** Flow-level visibility dashboard for EtherNet/IP (ENIP) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices.

**Network use case examples:** Supports industrial network capacity planning by monitoring EtherNet/IP connection counts and communication volumes across PLC and SCADA devices.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including ENIP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports ENIP connection counts and command frequency baselines for OT operational health monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks EtherNet/IP (ENIP) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices. **Network use case examples:** Helps identify top-volume sessions for ENIP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in EtherNet/IP (ENIP) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active ENIP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for EtherNet/IP (ENIP) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices. **Network use case examples:** Supports capacity planning by showing ENIP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all EtherNet/IP (ENIP) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate ICS protocol abuse, unauthorised PLC commands, and industrial network reconnaissance targeting ENIP devices. **Network use case examples:** Provides an operational log view for ENIP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-FTP

**Security use case examples:** Flow-level visibility dashboard for FTP (File Transfer Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers.

**Network use case examples:** Enables FTP server capacity planning by tracking session counts, active client cardinality, and data transfer volume trends for storage and bandwidth sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including FTP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports FTP server load, session counts, and data transfer volume baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks FTP (File Transfer Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers. **Network use case examples:** Helps identify top-volume sessions for FTP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in FTP (File Transfer Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active FTP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for FTP (File Transfer Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers. **Network use case examples:** Supports capacity planning by showing FTP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all FTP (File Transfer Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate cleartext credential exposure, data exfiltration via FTP, and unauthorised file transfers. **Network use case examples:** Provides an operational log view for FTP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-FTPDATA

**Security use case examples:** Flow-level visibility dashboard for FTP data channel traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of large-volume FTP data exfiltration and anomalous file transfer sessions.

**Network use case examples:** Supports FTP data transfer capacity planning by tracking data channel session counts, transfer volumes, and bandwidth utilisation trends over time.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including FTPDATA flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate large-volume FTP data exfiltration and anomalous file transfer sessions as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports FTP data channel throughput and peak transfer capacity planning within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks FTP data channel flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with large-volume FTP data exfiltration and anomalous file transfer sessions. **Network use case examples:** Helps identify top-volume sessions for FTPDATA traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in FTP data channel flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, large-volume FTP data exfiltration and anomalous file transfer sessions, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active FTPDATA communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for FTP data channel sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with large-volume FTP data exfiltration and anomalous file transfer sessions. **Network use case examples:** Supports capacity planning by showing FTPDATA session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all FTP data channel flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate large-volume FTP data exfiltration and anomalous file transfer sessions. **Network use case examples:** Provides an operational log view for FTPDATA flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-HTTP

**Security use case examples:** Flow-level visibility dashboard for HTTP traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP.

**Network use case examples:** Enables web infrastructure capacity planning by tracking HTTP session counts, active endpoint pairs, and connection duration trends for server dimensioning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including HTTP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports web traffic volume trending and application bandwidth utilisation within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks HTTP flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP. **Network use case examples:** Helps identify top-volume sessions for HTTP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in HTTP flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active HTTP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for HTTP sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP. **Network use case examples:** Supports capacity planning by showing HTTP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all HTTP flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate web-based exploitation, malware delivery, suspicious user agents, and data exfiltration over HTTP. **Network use case examples:** Provides an operational log view for HTTP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-HTTP2

**Security use case examples:** Flow-level visibility dashboard for HTTP/2 traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour.

**Network use case examples:** Supports HTTP/2 service capacity planning by monitoring multiplexed connection counts, session durations, and active endpoint cardinality across web infrastructure.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including HTTP2 flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports HTTP/2 adoption rates and connection efficiency monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks HTTP/2 flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour. **Network use case examples:** Helps identify top-volume sessions for HTTP2 traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in HTTP/2 flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active HTTP2 communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for HTTP/2 sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour. **Network use case examples:** Supports capacity planning by showing HTTP2 session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all HTTP/2 flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate HTTP/2-tunneled C2 traffic, protocol abuse, and anomalous multiplexed stream behaviour. **Network use case examples:** Provides an operational log view for HTTP2 flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-HUNT-ICMP-Exfil

**Security use case examples:** Purpose-built threat hunting dashboard for detecting ICMP-based data exfiltration and covert C2 channels by analysing flow asymmetry, transaction counts, and session durations.

**Network use case examples:** Supports network baseline validation by establishing expected ICMP flow size and duration norms, enabling policy enforcement and anomaly threshold definition for ICMP traffic.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time with `app_proto` as the series key, providing context on the protocol mix surrounding ICMP traffic under investigation. **Security use case examples:** A sudden spike in ICMP flows relative to other protocols is an early indicator of ICMP tunneling, covert channel activity, or network scanning campaigns. **Network use case examples:** Supports operational baselines for normal ICMP-to-other-protocol ratios on the monitored segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-FLOW-ICMP-Exfil-Hunt-1** | **Generic description:** This data table lists ICMP flows sorted by flow age and bytes-to-client (`flow.bytes_toclient`), showing `flow_id` for correlation, targeting slow or large ICMP sessions characteristic of data exfiltration via ICMP tunneling. **Security use case examples:** Unusually high `flow.bytes_toclient` values relative to flow age are a strong indicator of covert ICMP channel use for C2 or data theft. **Network use case examples:** Supports network baseline validation by highlighting ICMP flows that deviate from expected size and duration norms. | `flow.age`, `flow.bytes_toclient`, `flow_id` | event_type:flow |
| **SN-FLOW-ICMP-UnequalClientServerSize-Hunt-1** | **Generic description:** This data table surfaces ICMP flows where `flow.bytes_toclient` and `flow.bytes_toserver` are significantly asymmetric, alongside `flow.age` and `flow_id`, to detect tunneling by volume imbalance characteristic of exfiltration or covert channels. **Security use case examples:** Legitimate ICMP traffic (ping) has near-equal bidirectional byte counts; unequal sizes strongly suggest payload embedding for data exfiltration or C2 communications. **Network use case examples:** Supports traffic normalcy validation by quantifying ICMP asymmetry as a policy enforcement metric. | `flow.age`, `flow.bytes_toclient`, `flow.bytes_toserver`, `flow_id` | event_type:flow |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks ICMP flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** Anomalously high transaction counts in ICMP flows indicate automated tunneling tools sending repeated payloads, a hallmark of covert C2 channels. **Network use case examples:** Helps identify top-volume ICMP sessions for traffic normalcy validation and capacity impact assessment of ICMP-heavy traffic. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This line chart tracks the unique count of source and destination IP addresses in ICMP flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** A spike in unique ICMP-communicating hosts indicates scanning activity or a spreading ICMP-tunnel infection across the network. **Network use case examples:** Provides a topology-visibility baseline for the number of active ICMP communicating endpoints at any given time. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots mean ICMP flow duration (`flow.age`) and total flow count over time, enabling identification of unusually persistent ICMP sessions. **Security use case examples:** Long-lived ICMP flows with sustained traffic are strong indicators of active tunneling or C2 beaconing using ICMP as a covert channel. **Network use case examples:** Supports capacity planning by surfacing the count and longevity of active ICMP sessions for operational health assessment. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all ICMP flow records, enabling analysts to drill into individual sessions with full field detail for ICMP exfiltration and tunneling investigations. **Security use case examples:** Pivoting on `flow.bytes_toclient`, `flow.age`, and `flow_id` allows rapid triage of suspicious ICMP sessions identified in the hunting panels above. **Network use case examples:** Supports ad-hoc ICMP traffic auditing and forensic investigation of covert channel activity. | List of FLOW events | — |

## Dashboard: SN-FLOW-IKE

**Security use case examples:** Flow-level visibility dashboard for IKE (Internet Key Exchange) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts.

**Network use case examples:** Enables VPN gateway capacity planning by tracking IKE session counts, negotiation rates, and active tunnel endpoint cardinality for infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including IKE flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports VPN gateway session load and IKE negotiation volume monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks IKE (Internet Key Exchange) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts. **Network use case examples:** Helps identify top-volume sessions for IKE traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in IKE (Internet Key Exchange) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active IKE communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for IKE (Internet Key Exchange) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts. **Network use case examples:** Supports capacity planning by showing IKE session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all IKE (Internet Key Exchange) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate VPN misconfiguration, weak IKE proposals, and unauthorised VPN tunnel establishment attempts. **Network use case examples:** Provides an operational log view for IKE flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-IMAP

**Security use case examples:** Flow-level visibility dashboard for IMAP (Internet Message Access Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of mailbox credential stuffing, email harvesting, and account takeover via IMAP.

**Network use case examples:** Supports mail server capacity planning by monitoring IMAP session counts, active client cardinality, and connection duration trends for service dimensioning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including IMAP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate mailbox credential stuffing, email harvesting, and account takeover via IMAP as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports mail server IMAP session counts and per-user connection frequency baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks IMAP (Internet Message Access Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with mailbox credential stuffing, email harvesting, and account takeover via IMAP. **Network use case examples:** Helps identify top-volume sessions for IMAP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in IMAP (Internet Message Access Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, mailbox credential stuffing, email harvesting, and account takeover via IMAP, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active IMAP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for IMAP (Internet Message Access Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with mailbox credential stuffing, email harvesting, and account takeover via IMAP. **Network use case examples:** Supports capacity planning by showing IMAP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all IMAP (Internet Message Access Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate mailbox credential stuffing, email harvesting, and account takeover via IMAP. **Network use case examples:** Provides an operational log view for IMAP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-KRB5

**Security use case examples:** Flow-level visibility dashboard for Kerberos (KRB5) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks.

**Network use case examples:** Enables Kerberos infrastructure capacity planning by monitoring authentication session counts and ticket exchange rates to support directory service dimensioning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including KRB5 flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports Kerberos authentication volume and ticket-granting service load monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks Kerberos (KRB5) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks. **Network use case examples:** Helps identify top-volume sessions for KRB5 traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in Kerberos (KRB5) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active KRB5 communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for Kerberos (KRB5) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks. **Network use case examples:** Supports capacity planning by showing KRB5 session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all Kerberos (KRB5) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate pass-the-ticket, golden ticket forgery, Kerberoasting, and AS-REP roasting attacks. **Network use case examples:** Provides an operational log view for KRB5 flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-LDAP

**Security use case examples:** Flow-level visibility dashboard for LDAP (Lightweight Directory Access Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance.

**Network use case examples:** Supports directory service capacity planning by tracking LDAP session counts, query rates, and active client endpoint cardinality for AD infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including LDAP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports LDAP query volumes, directory replication traffic, and authentication service load baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks LDAP (Lightweight Directory Access Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance. **Network use case examples:** Helps identify top-volume sessions for LDAP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in LDAP (Lightweight Directory Access Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active LDAP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for LDAP (Lightweight Directory Access Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance. **Network use case examples:** Supports capacity planning by showing LDAP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all LDAP (Lightweight Directory Access Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate directory enumeration, credential stuffing against LDAP, LDAP injection, and AD reconnaissance. **Network use case examples:** Provides an operational log view for LDAP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-MDNS

**Security use case examples:** Flow-level visibility dashboard for mDNS (Multicast DNS) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse.

**Network use case examples:** Enables local network service discovery planning by monitoring mDNS traffic volumes and responder counts across network segments for multicast capacity management.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including MDNS flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports local network service discovery traffic volumes and multicast DNS responder inventory within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks mDNS (Multicast DNS) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse. **Network use case examples:** Helps identify top-volume sessions for MDNS traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in mDNS (Multicast DNS) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active MDNS communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for mDNS (Multicast DNS) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse. **Network use case examples:** Supports capacity planning by showing MDNS session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all mDNS (Multicast DNS) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate network reconnaissance via mDNS enumeration, rogue mDNS responders, and service discovery abuse. **Network use case examples:** Provides an operational log view for MDNS flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-MQTT

**Security use case examples:** Flow-level visibility dashboard for MQTT (Message Queuing Telemetry Transport) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data.

**Network use case examples:** Supports IoT broker capacity planning by tracking MQTT session counts, message rates, and active device cardinality to size broker infrastructure appropriately.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including MQTT flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports MQTT broker message throughput and connected client count baseline monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks MQTT (Message Queuing Telemetry Transport) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data. **Network use case examples:** Helps identify top-volume sessions for MQTT traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in MQTT (Message Queuing Telemetry Transport) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active MQTT communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for MQTT (Message Queuing Telemetry Transport) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data. **Network use case examples:** Supports capacity planning by showing MQTT session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all MQTT (Message Queuing Telemetry Transport) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate IoT device compromise, MQTT broker abuse, and unauthorised topic subscriptions exfiltrating sensor data. **Network use case examples:** Provides an operational log view for MQTT flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-NFS

**Security use case examples:** Flow-level visibility dashboard for NFS (Network File System) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies.

**Network use case examples:** Enables NFS storage infrastructure capacity planning by monitoring session counts, data transfer volumes, and active mount endpoint cardinality for storage sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including NFS flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports NFS mount counts, data transfer volumes, and server load baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks NFS (Network File System) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies. **Network use case examples:** Helps identify top-volume sessions for NFS traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in NFS (Network File System) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active NFS communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for NFS (Network File System) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies. **Network use case examples:** Supports capacity planning by showing NFS session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all NFS (Network File System) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate unauthorised NFS mount attempts, data exfiltration via NFS, and misconfigured export policies. **Network use case examples:** Provides an operational log view for NFS flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-NTP

**Security use case examples:** Flow-level visibility dashboard for NTP (Network Time Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise.

**Network use case examples:** Supports time synchronisation infrastructure planning by tracking NTP session counts, query rates, and active client cardinality for server capacity management.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including NTP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports time synchronisation server load, client query rates, and NTP infrastructure health baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks NTP (Network Time Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise. **Network use case examples:** Helps identify top-volume sessions for NTP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in NTP (Network Time Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active NTP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for NTP (Network Time Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise. **Network use case examples:** Supports capacity planning by showing NTP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all NTP (Network Time Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate NTP amplification DDoS attacks, rogue NTP servers, and time-synchronisation drift indicating compromise. **Network use case examples:** Provides an operational log view for NTP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-POP3

**Security use case examples:** Flow-level visibility dashboard for POP3 (Post Office Protocol v3) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover.

**Network use case examples:** Enables mail server capacity planning by tracking POP3 session counts, active client cardinality, and connection duration trends for legacy mail infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including POP3 flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports POP3 session volume trends and legacy mail client inventory monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks POP3 (Post Office Protocol v3) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover. **Network use case examples:** Helps identify top-volume sessions for POP3 traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in POP3 (Post Office Protocol v3) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active POP3 communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for POP3 (Post Office Protocol v3) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover. **Network use case examples:** Supports capacity planning by showing POP3 session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all POP3 (Post Office Protocol v3) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate cleartext credential interception, email harvesting, and legacy protocol abuse for account takeover. **Network use case examples:** Provides an operational log view for POP3 flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-QUIC

**Security use case examples:** Flow-level visibility dashboard for QUIC (Quick UDP Internet Connections) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection.

**Network use case examples:** Supports QUIC adoption monitoring and network capacity planning by tracking session counts, active endpoint pairs, and protocol distribution trends.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including QUIC flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports QUIC adoption rates, UDP port 443 traffic baselines, and application protocol distribution within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks QUIC (Quick UDP Internet Connections) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection. **Network use case examples:** Helps identify top-volume sessions for QUIC traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in QUIC (Quick UDP Internet Connections) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active QUIC communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for QUIC (Quick UDP Internet Connections) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection. **Network use case examples:** Supports capacity planning by showing QUIC session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all QUIC (Quick UDP Internet Connections) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate QUIC-tunneled C2 traffic, encrypted malware delivery, and evasion of HTTP/TLS inspection. **Network use case examples:** Provides an operational log view for QUIC flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-RDP

**Security use case examples:** Flow-level visibility dashboard for RDP (Remote Desktop Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP.

**Network use case examples:** Enables remote access infrastructure capacity planning by tracking RDP session counts, active endpoint pairs, and connection duration trends for gateway sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including RDP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports RDP session counts, concurrent connection limits, and remote access policy enforcement within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks RDP (Remote Desktop Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP. **Network use case examples:** Helps identify top-volume sessions for RDP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in RDP (Remote Desktop Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active RDP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for RDP (Remote Desktop Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP. **Network use case examples:** Supports capacity planning by showing RDP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all RDP (Remote Desktop Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate brute-force logins, BlueKeep-style exploitation, lateral movement, and ransomware delivery over RDP. **Network use case examples:** Provides an operational log view for RDP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-RFB

**Security use case examples:** Flow-level visibility dashboard for RFB/VNC (Remote Framebuffer Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC.

**Network use case examples:** Supports remote management infrastructure planning by tracking VNC session counts, active endpoint cardinality, and session duration trends for support capacity sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including RFB flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports VNC session counts and remote-access endpoint inventory visibility within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks RFB/VNC (Remote Framebuffer Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC. **Network use case examples:** Helps identify top-volume sessions for RFB traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in RFB/VNC (Remote Framebuffer Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active RFB communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for RFB/VNC (Remote Framebuffer Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC. **Network use case examples:** Supports capacity planning by showing RFB session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all RFB/VNC (Remote Framebuffer Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate unauthorised VNC access, cleartext remote desktop session exposure, and lateral movement via VNC. **Network use case examples:** Provides an operational log view for RFB flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-SIP

**Security use case examples:** Flow-level visibility dashboard for SIP (Session Initiation Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking.

**Network use case examples:** Enables VoIP infrastructure capacity planning by tracking SIP session counts, active endpoint pairs, and call flow duration trends for trunk and gateway dimensioning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including SIP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports VoIP call volume, trunk utilisation, and SIP session capacity planning within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks SIP (Session Initiation Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking. **Network use case examples:** Helps identify top-volume sessions for SIP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in SIP (Session Initiation Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active SIP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for SIP (Session Initiation Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking. **Network use case examples:** Supports capacity planning by showing SIP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all SIP (Session Initiation Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate VoIP enumeration, toll fraud, SIP invite floods, and unauthorised session hijacking. **Network use case examples:** Provides an operational log view for SIP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-SMB

**Security use case examples:** Flow-level visibility dashboard for SMB (Server Message Block) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares.

**Network use case examples:** Supports file sharing infrastructure capacity planning by tracking SMB session counts, active client-server pairs, and data transfer volume trends for server sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including SMB flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports file share utilisation, SMB session counts, and storage access capacity within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks SMB (Server Message Block) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares. **Network use case examples:** Helps identify top-volume sessions for SMB traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in SMB (Server Message Block) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active SMB communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for SMB (Server Message Block) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares. **Network use case examples:** Supports capacity planning by showing SMB session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all SMB (Server Message Block) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate lateral movement, ransomware propagation, and credential relay attacks exploiting SMB shares. **Network use case examples:** Provides an operational log view for SMB flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-SMTP

**Security use case examples:** Flow-level visibility dashboard for SMTP (Simple Mail Transfer Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of phishing delivery infrastructure, spam campaigns, and email-based malware distribution.

**Network use case examples:** Enables mail relay capacity planning by tracking SMTP session counts, active endpoint pairs, and message flow duration trends for MTA infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including SMTP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate phishing delivery infrastructure, spam campaigns, and email-based malware distribution as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports mail server throughput capacity planning and outbound relay health within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks SMTP (Simple Mail Transfer Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with phishing delivery infrastructure, spam campaigns, and email-based malware distribution. **Network use case examples:** Helps identify top-volume sessions for SMTP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in SMTP (Simple Mail Transfer Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, phishing delivery infrastructure, spam campaigns, and email-based malware distribution, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active SMTP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for SMTP (Simple Mail Transfer Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with phishing delivery infrastructure, spam campaigns, and email-based malware distribution. **Network use case examples:** Supports capacity planning by showing SMTP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all SMTP (Simple Mail Transfer Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate phishing delivery infrastructure, spam campaigns, and email-based malware distribution. **Network use case examples:** Provides an operational log view for SMTP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-SNMP

**Security use case examples:** Flow-level visibility dashboard for SNMP (Simple Network Management Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration.

**Network use case examples:** Supports network management infrastructure planning by tracking SNMP session counts, active device pairs, and polling frequency trends for management plane capacity.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including SNMP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports network device management traffic baseline and SNMP polling load monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks SNMP (Simple Network Management Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration. **Network use case examples:** Helps identify top-volume sessions for SNMP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in SNMP (Simple Network Management Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active SNMP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for SNMP (Simple Network Management Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration. **Network use case examples:** Supports capacity planning by showing SNMP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all SNMP (Simple Network Management Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate community string brute-forcing, SNMP amplification in DDoS, and device configuration enumeration. **Network use case examples:** Provides an operational log view for SNMP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-SSH

**Security use case examples:** Flow-level visibility dashboard for SSH (Secure Shell) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of brute-force attacks, lateral movement, and unauthorised remote access over SSH.

**Network use case examples:** Enables remote access infrastructure capacity planning by tracking SSH session counts, active endpoint pairs, and connection duration baselines for gateway sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including SSH flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate brute-force attacks, lateral movement, and unauthorised remote access over SSH as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports SSH session volume and endpoint access pattern baseline monitoring within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks SSH (Secure Shell) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with brute-force attacks, lateral movement, and unauthorised remote access over SSH. **Network use case examples:** Helps identify top-volume sessions for SSH traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in SSH (Secure Shell) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, brute-force attacks, lateral movement, and unauthorised remote access over SSH, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active SSH communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for SSH (Secure Shell) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with brute-force attacks, lateral movement, and unauthorised remote access over SSH. **Network use case examples:** Supports capacity planning by showing SSH session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all SSH (Secure Shell) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate brute-force attacks, lateral movement, and unauthorised remote access over SSH. **Network use case examples:** Provides an operational log view for SSH flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-TFTP

**Security use case examples:** Flow-level visibility dashboard for TFTP (Trivial File Transfer Protocol) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of firmware implantation, malware staging over TFTP, and unauthorised file retrievals.

**Network use case examples:** Supports firmware distribution and configuration management planning by tracking TFTP session counts and transfer volume trends across network devices.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including TFTP flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate firmware implantation, malware staging over TFTP, and unauthorised file retrievals as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports TFTP transfer activity and server utilisation baselines within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks TFTP (Trivial File Transfer Protocol) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with firmware implantation, malware staging over TFTP, and unauthorised file retrievals. **Network use case examples:** Helps identify top-volume sessions for TFTP traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in TFTP (Trivial File Transfer Protocol) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, firmware implantation, malware staging over TFTP, and unauthorised file retrievals, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active TFTP communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for TFTP (Trivial File Transfer Protocol) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with firmware implantation, malware staging over TFTP, and unauthorised file retrievals. **Network use case examples:** Supports capacity planning by showing TFTP session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all TFTP (Trivial File Transfer Protocol) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate firmware implantation, malware staging over TFTP, and unauthorised file retrievals. **Network use case examples:** Provides an operational log view for TFTP flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-TLS

**Security use case examples:** Flow-level visibility dashboard for TLS (Transport Layer Security) traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks.

**Network use case examples:** Enables TLS service capacity planning by tracking encrypted session counts, active endpoint pairs, and connection duration trends for load balancer and proxy sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including TLS flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports TLS version and cipher suite compliance across the network within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks TLS (Transport Layer Security) flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks. **Network use case examples:** Helps identify top-volume sessions for TLS traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in TLS (Transport Layer Security) flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active TLS communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for TLS (Transport Layer Security) sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks. **Network use case examples:** Supports capacity planning by showing TLS session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all TLS (Transport Layer Security) flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate weak cipher negotiation, expired certificates, TLS-based C2 channels, and protocol downgrade attacks. **Network use case examples:** Provides an operational log view for TLS flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-FLOW-WEBSOCKET

**Security use case examples:** Flow-level visibility dashboard for WebSocket traffic, providing session counts, IP pair cardinality, flow duration trends, and transaction rankings to support detection of WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections.

**Network use case examples:** Supports WebSocket service capacity planning by monitoring persistent connection counts, session durations, and active endpoint cardinality for application server sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Application protocol** | **Generic description:** This stacked bar chart aggregates network flows by application protocol over time, with `app_proto` as the series key, showing the protocol mix including WEBSOCKET flows. **Security use case examples:** Sudden spikes or protocol-ratio shifts may indicate WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections as well as unexpected protocol usage signalling policy violations. **Network use case examples:** Supports WebSocket connection counts, session durations, and long-lived connection capacity planning within the monitored network segment. | `timestamp`, `app_proto` | event_type:all |
| **SN-Flow-Txcnt** | **Generic description:** This data table ranks WebSocket flows by transaction count (`flow.tx_cnt`), showing `app_proto` and `flow_id` for the most active sessions. **Security use case examples:** High transaction counts in a single flow may indicate scanning, automated exploitation, or persistent connections associated with WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections. **Network use case examples:** Helps identify top-volume sessions for WEBSOCKET traffic shaping and bandwidth capacity reviews. | `app_proto`, `flow.tx_cnt`, `flow_id` | event_type:flow |
| **SN-Flow unique count of src and dst IP** | **Generic description:** This dual line chart tracks the unique count of source and destination IP addresses in WebSocket flows over time, plotting `src_ip` and `dest_ip` cardinality. **Security use case examples:** Sudden growth in unique IPs may indicate scanning, WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections, or infection spread. **Network use case examples:** Provides a topology-visibility baseline for the number of active WEBSOCKET communicating endpoints. | `dest_ip`, `src_ip`, `timestamp` | event_type:all |
| **SN-Mean flow age and count** | **Generic description:** This line chart plots the mean flow duration (`flow.age`) and total flow count for WebSocket sessions over time, enabling trend analysis of connection longevity. **Security use case examples:** Unusually long-lived flows can indicate persistent C2 channels or slow-exfiltration patterns associated with WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections. **Network use case examples:** Supports capacity planning by showing WEBSOCKET session count trends and average connection lifetime. | `flow.age`, `timestamp` | event_type:all |
| **SN-FLOW-EventsList (search)** | **Generic description:** This saved search presents a raw event table of all WebSocket flow records, enabling drill-down inspection of individual sessions with full field detail. **Security use case examples:** Analysts can filter and pivot on fields such as source/destination IP, port, and `flow.tx_cnt` to investigate WebSocket-based C2 channels, covert data exfiltration over persistent WebSocket connections. **Network use case examples:** Provides an operational log view for WEBSOCKET flow auditing and ad-hoc network troubleshooting. | List of FLOW events | — |

## Dashboard: SN-HTTP

**Security use case examples:** Provides comprehensive HTTP protocol monitoring for web application attack detection, C2 beacon identification, and outbound web traffic policy enforcement.

**Network use case examples:** Enables web traffic capacity planning and application performance monitoring through HTTP response code distribution, URI path analysis, and server-side traffic volume trending.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-HTTP methods** | **Generic description:** Donut chart showing the distribution of HTTP request methods (GET, POST, PUT, DELETE, HEAD, OPTIONS, CONNECT, TRACE, PATCH). **Security use case examples:** Unusual method distributions — high CONNECT rates (proxy tunnelling), elevated PUT/DELETE (API exploitation), or TRACE (XST attacks) — indicate potential web application abuse. **Network use case examples:** Supports HTTP policy enforcement by providing visibility into which HTTP methods are used across the monitored network, enabling method-level firewall and WAF policy tuning. | `http.http_method` | event_type:http |
| **SN-HTTP-AcceptEncoding** | **Generic description:** Donut chart showing the distribution of HTTP Accept-Encoding header values across HTTP requests. **Security use case examples:** Unusual or absent Accept-Encoding values can fingerprint non-browser HTTP clients, including malware downloaders, automated scanners, and C2 beacon frameworks. **Network use case examples:** Provides an HTTP client behaviour baseline for user-agent and header policy enforcement, supporting web gateway configuration validation. | `http.accept_encoding` | event_type:http |
| **SN-HTTP-AcceptEncodingByConnection** | **Generic description:** Donut chart showing Accept-Encoding distribution correlated with HTTP connection type (keep-alive, close, etc.). **Security use case examples:** Malware using raw HTTP libraries often pairs unusual Accept-Encoding with non-standard connection headers, creating a combinatorial fingerprint for detection. **Network use case examples:** Supports web proxy policy validation by identifying non-compliant HTTP client configurations that deviate from standard browser behaviour patterns. | `http.http_accept_encoding`, `http.connection` | event_type:http |
| **SN-HTTP-CacheControl** | **Generic description:** Donut chart showing the distribution of Cache-Control header values in HTTP requests and responses. **Security use case examples:** Cache-Control: no-store or no-cache headers can be used by malware to prevent proxy caching of C2 payloads, potentially indicating tool-specific HTTP implementation patterns. **Network use case examples:** Tracks caching policy compliance across monitored web traffic, supporting decisions about CDN and proxy cache infrastructure effectiveness. | `http.http_cache_control` | event_type:http |
| **SN-HTTP-ContentTypeByAplication** | **Generic description:** Donut chart showing the distribution of HTTP Content-Type values, grouped by application category. **Security use case examples:** Unusual content types — such as application/octet-stream delivered via unexpected endpoints — may indicate file-based malware delivery or data exfiltration via HTTP POST. **Network use case examples:** Provides a content-type baseline for web application traffic, supporting DLP policy validation and content filtering rule development. | `http.http_content_type` | event_type:http |
| **SN-HTTP-EventsOverTime** | **Generic description:** Line chart showing HTTP event volume over time. **Security use case examples:** HTTP traffic spikes during off-hours or from unexpected source IPs may indicate automated scraping, brute-force attacks, or C2 beaconing activity. **Network use case examples:** Provides an HTTP traffic volume baseline for capacity planning, enabling infrastructure teams to correlate traffic peaks with application usage patterns. | List of HTTP events | event_type:http |
| **SN-HTTP-GeoIP** | **Generic description:** Geographic heatmap overlaying HTTP traffic source or destination geolocation on a world map. **Security use case examples:** Geographic anomalies in HTTP traffic origins — unexpected countries sending requests to internal web servers — may indicate scanning, exploitation attempts, or geo-policy bypasses. **Network use case examples:** Supports content delivery network planning and geo-based access policy enforcement by visualising the global distribution of HTTP client traffic. | `geoip.location` | event_type:http |
| **SN-HTTP-Servers** | **Generic description:** Donut chart showing the distribution of HTTP server header values from web server responses. **Security use case examples:** Unusual or unexpected server banners (e.g. uncommon web frameworks, outdated versions) may reveal unpatched software, unauthorised services, or obfuscated malware C2 servers. **Network use case examples:** Provides a web server software inventory for patch management and compliance, enabling operations teams to track server software distribution across the monitored network. | `http.server` | event_type:http |
| **SN-HTTP-StatusCode** | **Generic description:** Donut chart showing the distribution of HTTP response status codes (2xx, 3xx, 4xx, 5xx) across HTTP traffic. **Security use case examples:** Elevated 4xx or 5xx rates can indicate brute-force attempts, path traversal scanning, application errors under attack load, or misconfigured reverse proxies. **Network use case examples:** Provides an application health indicator for SLA monitoring, tracking error rate distributions and response code trends for web service availability management. | `http.status` | event_type:http |
| **SN-HTTP-Total** | **Generic description:** Single large-number metric displaying the total count of HTTP events in the selected time range. **Security use case examples:** Provides an immediate HTTP traffic volume indicator for detecting anomalous surges or drops relative to baseline, suggesting attack activity or service disruptions. **Network use case examples:** Serves as a key web traffic KPI for shift handover dashboards and capacity planning reports. | — | event_type:http |
| **SN-HTTP-UserAgentDevices** | **Generic description:** Donut chart showing HTTP user-agent traffic broken down by device type (Desktop, Mobile, Tablet, Bot, etc.). **Security use case examples:** Unexpected device type distributions — high bot traffic or unknown device categories — may indicate automated scanning tools, credential stuffing, or web scraping activity. **Network use case examples:** Supports capacity planning by understanding the device type mix of clients accessing monitored web services and validating mobile policy restrictions. | `http.user_agent.device` | event_type:http |
| **SN-HTTP-UserAgentMajor** | **Generic description:** Donut chart showing the distribution of HTTP user-agent strings by major version of identified browsers or applications. **Security use case examples:** Outdated browser versions appearing in user-agent data may indicate unpatched client endpoints, automated tools mimicking old browsers, or phishing kit traffic. **Network use case examples:** Provides browser version inventory data for patch management and end-of-life detection, supporting client software update policy enforcement. | `http.user_agent.major` | event_type:http |
| **SN-HTTP-UserAgentName** | **Generic description:** Donut chart showing HTTP traffic broken down by user-agent application name (Chrome, Firefox, curl, Python-requests, etc.). **Security use case examples:** Non-browser user-agents — particularly automated tools like curl, wget, or custom scripts — may indicate vulnerability scanning, credential stuffing, or malware HTTP clients. **Network use case examples:** Identifies the application mix making HTTP requests across the network, supporting software inventory and access policy decisions for web-facing services. | `http.http_user_agent` | event_type:http |
| **SN-HTTP-UserAgentOS** | **Generic description:** Donut chart showing the distribution of operating systems reported in HTTP user-agent strings. **Security use case examples:** Unexpected OS types in the environment — particularly unusual or legacy OS versions — may indicate BYOD policy violations, virtual machine misuse, or spoofed user-agents. **Network use case examples:** Supports endpoint compliance monitoring by providing OS distribution data from HTTP traffic, supplementing endpoint management system inventories. | `http.http_user_agent.os` | event_type:http |
| **SN-HTTP-Vary** | **Generic description:** Donut chart showing the distribution of HTTP Vary header values from server responses. **Security use case examples:** Unexpected Vary values may reveal server-side fingerprinting, proxy evasion, or non-standard application behaviour. **Network use case examples:** Tracks server caching policy diversity across monitored web services, supporting content delivery optimisation and proxy cache configuration validation. | `http.http_vary` | event_type:http |
| **SN-HTTP2-RequestSettings** | **Generic description:** Donut chart showing the distribution of HTTP/2 request settings frames, capturing protocol-level negotiation parameters. **Security use case examples:** Unusual HTTP/2 settings or frame patterns may indicate protocol-level attacks, misconfigured clients, or tools exploiting HTTP/2 vulnerabilities. **Network use case examples:** Tracks HTTP/2 adoption and configuration patterns across the network, supporting network modernisation planning and HTTP/2-specific capacity management. | `http.http2.request.settings.settings_id` | event_type:http |
| **SN-ThreatHunt-HTTP-PossibleC2Beacons-BySrcIP** | **Generic description:** Data table ranking source IPs by the number of HTTP requests showing beacon-like regularity — repeated connections at consistent intervals to the same destination. **Security use case examples:** Consistent, low-dwell HTTP beaconing is a hallmark of malware C2 communication; isolating source IPs with high beacon scores accelerates host triage and incident scope determination. **Network use case examples:** Provides a baseline for outbound HTTP communication patterns per endpoint, supporting change detection when new devices begin establishing regular outbound connections. | `http.length`, `http.hostname`, `src_ip` | event_type:http |
| **SN-HTTP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HTTP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of HTTP events | — |

## Dashboard: SN-HUNT-1

**Security use case examples:** Aggregates multi-protocol threat hunting panels combining flow, TLS, DNS, HTTP, anomaly, and alert data to support proactive threat detection across the full kill chain.

**Network use case examples:** Provides multi-protocol session and flow data useful for establishing cross-protocol traffic baselines and identifying anomalous endpoint behaviour patterns across the network.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-ANOMALY-ByAppProto** | **Generic description:** Donut chart showing the distribution of anomaly events by application-layer protocol. **Security use case examples:** Protocol-specific anomaly concentrations identify which application parsers are generating the most exceptions, indicating targeted evasion or parser misconfiguration. **Network use case examples:** Supports Suricata decoder tuning by identifying which protocols generate the highest anomaly rates, enabling focused configuration improvements. | — | event_type:anomaly |
| **SN-ANOMALY-EventType** | **Generic description:** Donut chart showing the distribution of Suricata anomaly event types (decoder, stream, applayer). **Security use case examples:** The breakdown between decoder, stream, and application-layer anomaly types reveals whether anomalies are low-level protocol violations or higher-level application parsing failures. **Network use case examples:** Provides anomaly event type taxonomy for sensor health reporting and prioritises Suricata configuration tuning efforts for the highest-impact anomaly categories. | `anomaly.event` | event_type:anomaly |
| **SN-Application protocol** | **Generic description:** Stacked bar chart showing event counts broken down by application-layer protocol (app_proto) over time. **Security use case examples:** Unexpected protocols appearing in flow data — or sudden drops in expected protocols — can signal service failures, protocol tunnelling, or exfiltration over non-standard channels. **Network use case examples:** Provides a comprehensive protocol inventory for capacity planning, enabling operations teams to track protocol adoption trends and validate that decommissioned protocols have been removed. | `timestamp`, `app_proto` | event_type:all |
| **SN-DNS-Rrname** | **Generic description:** Donut chart showing the top resolved domain names (rrname/query name field) by query frequency. **Security use case examples:** High-frequency queries to unusual, randomised, or encoded domain names are strong indicators of DGA activity, DNS tunnelling, or beaconing behaviour. **Network use case examples:** Provides a DNS query inventory for network access policy validation, confirming that internal hosts are only resolving expected domains and not bypassing DNS controls. | `dns.rrname` | event_type:dns |
| **SN-FILE-ByAppProto** | **Generic description:** Donut chart showing the distribution of file transactions by application-layer protocol (HTTP, SMTP, FTP, SMB, etc.). **Security use case examples:** Unexpected protocols carrying file transfers may indicate covert exfiltration channels or protocol misuse for staging malware or data theft. **Network use case examples:** Provides a file transfer protocol inventory for data governance and DLP policy development, showing which protocols carry the most file transfer activity. | `app_proto` | event_type:fileinfo |
| **SN-FILE-ByTypeOverTime** | **Generic description:** Stacked line chart showing file transaction counts broken down by file type (MIME type or extension) over time. **Security use case examples:** Sudden spikes in executable (.exe, .dll, .ps1) or document (.pdf, .doc, .xls) transfers may indicate malware distribution campaigns or targeted file-based attack activity. **Network use case examples:** Provides a file type trend baseline for DLP and content filtering policy reviews, enabling operations teams to track which file categories dominate transfer activity. | `timestamp`, `fileinfo.magic` | event_type:fileinfo |
| **SN-HTTP Top hostnames** | **Generic description:** Donut chart showing the most frequently accessed HTTP hostnames (Host header values) across all HTTP traffic. **Security use case examples:** Top hostnames in HTTP traffic reveal the most-contacted web services; unknown or suspicious domains in this list may indicate malware beacon destinations or data exfiltration targets. **Network use case examples:** Enables web traffic baseline establishment for network monitoring, tracking domain access patterns and supporting outbound web access policy development. | `http.hostname` | event_type:http |
| **SN-HTTP Top user agents** | **Generic description:** Donut chart showing the most frequently observed HTTP user-agent strings across all HTTP traffic. **Security use case examples:** The top user-agents list reveals the dominant client software mix; unusual entries like automated tool signatures or randomised UA strings indicate potentially malicious traffic. **Network use case examples:** Provides a client software landscape view for asset management and supports web access policy enforcement based on approved user-agent strings. | `http.http_user_agent` | event_type:http |
| **SN-HTTP-Servers** | **Generic description:** Donut chart showing the distribution of HTTP server header values from web server responses. **Security use case examples:** Unusual or unexpected server banners (e.g. uncommon web frameworks, outdated versions) may reveal unpatched software, unauthorised services, or obfuscated malware C2 servers. **Network use case examples:** Provides a web server software inventory for patch management and compliance, enabling operations teams to track server software distribution across the monitored network. | `http.server` | event_type:http |
| **SN-Mean flow age and count** | **Generic description:** Dual-axis line chart tracking mean flow duration (age) and total flow count simultaneously over time. **Security use case examples:** Unusually long-lived flows may indicate persistent C2 connections, data exfiltration sessions, or abandoned connections; short mean ages with high counts can signal scanning. **Network use case examples:** Provides flow lifecycle metrics for network capacity planning, helping operations teams understand connection duration patterns and session table sizing requirements. | `flow.age`, `timestamp` | event_type:all |
| **SN-RDP-ClientCookie** | **Generic description:** Donut chart showing the distribution of RDP ClientCookie data. **Security use case examples:** Analysis of RDP clientcookie distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientcookie patterns align with expected configurations. | `rdp.cookie` | event_type:rdp |
| **SN-SMB-NtlmsspHost** | **Generic description:** Donut chart showing the distribution of SMB NtlmsspHost data. **Security use case examples:** Analysis of SMB ntlmssphost distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that ntlmssphost patterns align with expected configurations. | `smb.ntlmssp.host` | event_type:smb |
| **SN-SMB-NtlmsspUser** | **Generic description:** Donut chart showing the distribution of SMB NtlmsspUser data. **Security use case examples:** Analysis of SMB ntlmsspuser distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that ntlmsspuser patterns align with expected configurations. | `smb.ntlmssp.user` | event_type:smb |
| **SN-SSH-ByClientHashByClientIPByPort** | **Generic description:** Hierarchical data table showing SSH client key hashes grouped by client IP and port, tracking which client keys are used from which hosts. **Security use case examples:** Unexpected client key hashes from known IP addresses may indicate key compromise, credential sharing, or unauthorised key deployment on client endpoints. **Network use case examples:** Supports SSH client key lifecycle management and validates that key distribution policies are being followed across all managed endpoints. | `ssh.client.hassh.hash`, `src_ip`, `ssh.client.software_version`, `src_port`, `ether.src_mac` | event_type:ssh |
| **SN-SSH-ByServerHashByServerIPByPort** | **Generic description:** Hierarchical data table showing SSH server host key hashes grouped by server IP and port, enabling host key inventory and change detection. **Security use case examples:** Host key changes detected via this table may indicate SSH server replacement, MitM interception, or key rotation events that require validation and certificate management actions. **Network use case examples:** Enables SSH host key inventory management across the network infrastructure, supporting compliance requirements for SSH key rotation and server authentication auditing. | `ssh.server.hassh.hash`, `dest_ip`, `ssh.server.software_version`, `dest_port`, `ether.dest_mac` | event_type:ssh |
| **SN-ThreatHunt-ALERTS-MutlipleUniqueAlertOnDestIP** | **Generic description:** Data table ranking destination IPs by the count of unique IDS signatures triggered against them, surfacing heavily targeted hosts. **Security use case examples:** Destination hosts attracting many different alert signatures may be under active exploitation, serving as honeypot-like targets, or hosting vulnerable services being probed. **Network use case examples:** Supports network asset prioritisation by identifying servers receiving disproportionate scanning or attack traffic, informing patch management and firewall rule reviews. | `dest_ip`, `alert.signature` | event_type:alert |
| **SN-ThreatHunt-ALERTS-MutlipleUniqueAlertOnSrcIP** | **Generic description:** Data table ranking source IPs by the count of unique IDS alert signatures they triggered, highlighting hosts generating diverse alert types. **Security use case examples:** A host triggering many distinct signatures simultaneously often indicates active exploitation, malware multi-stage activity, or automated scanning tools — a high-priority triage signal. **Network use case examples:** Establishes a per-IP signature diversity baseline, enabling operations teams to set thresholds for automated escalation and prioritise endpoint investigation queues. | `src_ip`, `alert.signature` | event_type:alert |
| **SN-ThreatHunt-HTTP-PossibleC2Beacons-BySrcIP** | **Generic description:** Data table ranking source IPs by the number of HTTP requests showing beacon-like regularity — repeated connections at consistent intervals to the same destination. **Security use case examples:** Consistent, low-dwell HTTP beaconing is a hallmark of malware C2 communication; isolating source IPs with high beacon scores accelerates host triage and incident scope determination. **Network use case examples:** Provides a baseline for outbound HTTP communication patterns per endpoint, supporting change detection when new devices begin establishing regular outbound connections. | `http.length`, `http.hostname`, `src_ip` | event_type:http |
| **SN-TLS TCP ports** | **Generic description:** Donut chart showing the distribution of TLS TCP PORTS events data. **Security use case examples:** Analysis of TLS TCP PORTS event distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TLS TCP PORTS protocol monitoring for capacity planning and validates that traffic patterns align with expected configurations. | `dest_port` | event_type:tls |
| **SN-TLS versions** | **Generic description:** Donut chart showing the distribution of TLS VERSIONS events data. **Security use case examples:** Analysis of TLS VERSIONS event distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TLS VERSIONS protocol monitoring for capacity planning and validates that traffic patterns align with expected configurations. | `tls.version` | event_type:tls |
| **SN-TLS-ByJa3Hash** | **Generic description:** Ranked data table listing the top JA3 TLS client fingerprint hashes by occurrence count, derived by hashing client-hello parameters including cipher suites, extensions, and elliptic curves. **Security use case examples:** JA3 fingerprints identify the TLS client library regardless of the server being contacted; repeated hashes from unexpected binaries indicate malware, C2 clients, or policy-violating TLS implementations. **Network use case examples:** Enables network-wide TLS client library inventory for compliance and asset management, confirming that only approved client TLS stacks are in use across the environment. | `src_ip`, `tls.ja3.hash` | event_type:tls |
| **SN-TLS-ByJa3SHash** | **Generic description:** Ranked data table listing the top JA3S TLS server-response fingerprint hashes, computed from server-hello parameters including the selected cipher suite and extensions. **Security use case examples:** JA3S hashes fingerprint TLS server behaviour and can expose C2 servers, self-signed certificate infrastructure, or non-standard TLS stacks hiding behind legitimate IP addresses. **Network use case examples:** Supports server-side TLS policy auditing by inventorying server TLS configurations and identifying servers presenting non-standard cipher suite selections. | `dest_ip`, `tls.ja3s.hash`, `tls.sni` | event_type:tls |
| **SN-TLS-BySni** | **Generic description:** Donut chart showing the distribution of TLS SNI (Server Name Indication) values across TLS handshakes. **Security use case examples:** SNI values reveal the intended domain of encrypted TLS connections; unusual domains appearing in SNI data indicate potential C2 communication, phishing site access, or policy violations. **Network use case examples:** Supports outbound TLS policy enforcement by inventorying which domains internal hosts are establishing encrypted connections to, enabling domain-based policy validation. | `tls.sni` | event_type:tls |
| **SN-ANOMALY-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ANOMALY events | — |
| **SN-SMB-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SMB events | — |
| **SN-ALERT-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALERT events | — |
| **SN-ALL-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALL events | — |
| **SN-FILE-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of FILE events | — |
| **SN-FLOW-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of FLOW events | — |
| **SN-HTTP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of HTTP events | — |
| **SN-TLS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-HUNT-1 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of TLS events | — |

## Dashboard: SN-IDS

**Security use case examples:** Timelion-based IDS overview dashboard displaying alert category, severity, signature, and protocol trends for high-level threat posture monitoring.

**Network use case examples:** Enables NOC teams to track alert rate trends and distribution over time for sensor tuning, detection coverage validation, and capacity planning without requiring deep security expertise.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Timelion-Alert-Category** | **Generic description:** Timelion time-series chart overlaying alert counts broken down by category over the selected time window. **Security use case examples:** Highlights sudden spikes in specific alert categories — such as exploit attempts, policy violations, or reconnaissance — that may indicate an active attack phase. **Network use case examples:** Establishes baseline category volumes over time, enabling operations teams to identify category drift and validate IDS rule tuning over rolling periods. | — | event_type:alert |
| **SN-Timelion-Alert-Country** | **Generic description:** Timelion time-series chart plotting alert event counts per source or destination country over time. **Security use case examples:** Detects sudden surges in alerts from specific countries, which may indicate geo-targeted attacks, compromised foreign assets, or blocked traffic bypassing geo-restrictions. **Network use case examples:** Supports policy compliance audits by visualising cross-border traffic volumes and verifying that country-based ACLs are operating as expected. | — | event_type:alert |
| **SN-Timelion-Alert-Severity** | **Generic description:** Timelion time-series chart tracking alert counts separated by severity level (1–3) over time. **Security use case examples:** Allows rapid identification of severity escalation patterns — e.g. a rising trend of severity-1 critical alerts — indicating a worsening threat posture. **Network use case examples:** Assists capacity planning for SOC analyst workload by forecasting high-severity alert volumes across time intervals. | — | event_type:alert |
| **SN-Timelion-Protocols** | **Generic description:** Timelion time-series chart overlaying event counts for multiple network protocols simultaneously. **Security use case examples:** Sudden shifts in protocol distribution — e.g. spike in raw TCP while DNS drops — can signal protocol tunnelling, C2 channels, or infrastructure changes. **Network use case examples:** Enables network operations teams to track protocol mix trends for capacity planning and to validate that decommissioned protocols no longer appear in traffic. | — | event_type:all |
| **SN-Timelion-Signatures** | **Generic description:** Timelion time-series chart tracking the count of unique IDS/IPS signature triggers over the selected time range. **Security use case examples:** A rising number of unique signatures firing simultaneously often indicates broad scanning, exploitation, or a multi-stage attack rather than isolated noise. **Network use case examples:** Provides a historical view of rule effectiveness over time, supporting periodic rule set reviews and signature tuning workflows. | — | event_type:alert |
| **SN-ALERT-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-IDS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALERT events | — |

## Dashboard: SN-IKEv2

**Security use case examples:** Provides visibility into IKEv2 VPN negotiation traffic to detect VPN scanning, brute-force attacks, and unauthorised VPN gateway access.

**Network use case examples:** Supports VPN gateway capacity planning by tracking IKEv2 negotiation volumes, session counts, and proposal distribution to size VPN infrastructure appropriately.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-IKEv2-EventsOverTime** | **Generic description:** Bar chart showing IKEV2 event volume over time. **Security use case examples:** Tracking IKEV2 traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides IKEV2 traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of IKEV2 events | event_type:ikev2 |
| **SN-IKEv2-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of IKEV2 traffic. **Security use case examples:** Visualises geographic origins of IKEV2 connections to detect anomalous or unexpected source countries. **Network use case examples:** Supports geographic access policy validation by confirming IKEV2 traffic flows to expected locations. | `geoip.location` | event_type:ikev2 |
| **SN-IKEv2-Role** | **Generic description:** Donut chart showing the distribution of IKEV2 Role data. **Security use case examples:** Analysis of IKEV2 role distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports IKEV2 protocol monitoring for capacity planning and validates that role patterns align with expected configurations. | `ikev2.role` | event_type:ikev2 |
| **SN-IKEv2-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for IKEV2 connections. **Security use case examples:** Identifying the most active IKEV2 destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that IKEV2 traffic uses only expected destination IPs. | `dest_ip` | event_type:ikev2 |
| **SN-IKEv2-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for IKEV2 connections. **Security use case examples:** Identifying the most active IKEV2 destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that IKEV2 traffic uses only expected destination ports. | `dest_port` | event_type:ikev2 |
| **SN-IKEv2-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for IKEV2 connections. **Security use case examples:** Identifying the most active IKEV2 source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that IKEV2 traffic uses only expected source IPs. | `src_ip` | event_type:ikev2 |
| **SN-IKEv2-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for IKEV2 connections. **Security use case examples:** Identifying the most active IKEV2 source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that IKEV2 traffic uses only expected source ports. | `src_port` | event_type:ikev2 |
| **SN-IKEv2-Total** | **Generic description:** Single metric display showing the total count of IKEV2 events. **Security use case examples:** Provides an at-a-glance IKEV2 event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary IKEV2 traffic KPI for operational monitoring and capacity planning. | — | event_type:ikev2 |
| **SN-IKEv2-VerMajMinor** | **Generic description:** Donut chart showing the distribution of IKEV2 VerMajMinor data. **Security use case examples:** Analysis of IKEV2 vermajminor distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports IKEV2 protocol monitoring for capacity planning and validates that vermajminor patterns align with expected configurations. | `ikev2.version_major`, `ikev2.version_minor` | event_type:ikev2 |
| **SN-IKEv2-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-IKEv2 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of IKEV2 events | — |

## Dashboard: SN-KRB5

**Security use case examples:** Monitors Kerberos authentication traffic to detect Active Directory attacks including Kerberoasting, AS-REP Roasting, Pass-the-Ticket, and weak encryption usage.

**Network use case examples:** Enables Active Directory infrastructure monitoring by tracking Kerberos ticket request volumes and authentication rates for directory service capacity planning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-KRB5-ByCname** | **Generic description:** Donut chart showing the distribution of KRB5 ByCname data. **Security use case examples:** Analysis of KRB5 cname distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that cname patterns align with expected configurations. | `krb5.cname` | event_type:krb5 |
| **SN-KRB5-ByDestIP** | **Generic description:** Data table showing the distribution of KRB5 ByDestIP data. **Security use case examples:** Analysis of KRB5 destip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that destip patterns align with expected configurations. | `dest_ip` | event_type:krb5 |
| **SN-KRB5-ByDestPort** | **Generic description:** Data table showing the distribution of KRB5 ByDestPort data. **Security use case examples:** Analysis of KRB5 destport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that destport patterns align with expected configurations. | `dest_port` | event_type:krb5 |
| **SN-KRB5-ByEncryption** | **Generic description:** Donut chart showing the distribution of KRB5 ByEncryption data. **Security use case examples:** Analysis of KRB5 encryption distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that encryption patterns align with expected configurations. | `krb5.encryption` | event_type:krb5 |
| **SN-KRB5-ByErrCode** | **Generic description:** Donut chart showing the distribution of KRB5 ByErrCode data. **Security use case examples:** Analysis of KRB5 errcode distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that errcode patterns align with expected configurations. | `krb5.error_code` | event_type:krb5 |
| **SN-KRB5-ByFailedRequests** | **Generic description:** Donut chart showing the distribution of KRB5 ByFailedRequests data. **Security use case examples:** Analysis of KRB5 failedrequests distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that failedrequests patterns align with expected configurations. | `krb5.failed_request` | event_type:krb5 |
| **SN-KRB5-ByMsgType** | **Generic description:** Donut chart showing the distribution of KRB5 ByMsgType data. **Security use case examples:** Analysis of KRB5 msgtype distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that msgtype patterns align with expected configurations. | `krb5.msg_type` | event_type:krb5 |
| **SN-KRB5-ByRealm** | **Generic description:** Donut chart showing the distribution of KRB5 ByRealm data. **Security use case examples:** Analysis of KRB5 realm distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that realm patterns align with expected configurations. | `krb5.realm` | event_type:krb5 |
| **SN-KRB5-BySname** | **Generic description:** Donut chart showing the distribution of KRB5 BySname data. **Security use case examples:** Analysis of KRB5 sname distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that sname patterns align with expected configurations. | `krb5.sname` | event_type:krb5 |
| **SN-KRB5-BySrcIP** | **Generic description:** Data table showing the distribution of KRB5 BySrcIP data. **Security use case examples:** Analysis of KRB5 srcip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that srcip patterns align with expected configurations. | `src_ip` | event_type:krb5 |
| **SN-KRB5-BySrcPort** | **Generic description:** Data table showing the distribution of KRB5 BySrcPort data. **Security use case examples:** Analysis of KRB5 srcport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that srcport patterns align with expected configurations. | `src_port` | event_type:krb5 |
| **SN-KRB5-ByWeakEncryption** | **Generic description:** Donut chart showing the distribution of KRB5 ByWeakEncryption data. **Security use case examples:** Analysis of KRB5 weakencryption distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports KRB5 protocol monitoring for capacity planning and validates that weakencryption patterns align with expected configurations. | `krb5.weak_encryption` | event_type:krb5 |
| **SN-KRB5-EventsOverTime** | **Generic description:** Bar chart showing KRB5 event volume over time. **Security use case examples:** Tracking KRB5 traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides KRB5 traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of KRB5 events | event_type:krb5 |
| **SN-KRB5-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of KRB5 traffic. **Security use case examples:** Visualises geographic origins of KRB5 connections to detect anomalous or unexpected source countries. **Network use case examples:** Supports geographic access policy validation by confirming KRB5 traffic flows to expected locations. | `geoip.location` | event_type:krb5 |
| **SN-KRB5-TotalCount** | **Generic description:** Single metric display showing the total count of KRB5 events. **Security use case examples:** Provides an at-a-glance KRB5 event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary KRB5 traffic KPI for operational monitoring and capacity planning. | — | event_type:krb5 |
| **SN-KRB5-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-KRB5 events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of KRB5 events | — |

## Dashboard: SN-MQTT

**Security use case examples:** Provides visibility into MQTT IoT protocol communications to detect unauthorised broker access, compromised IoT devices, and MQTT-based C2 channels.

**Network use case examples:** Supports IoT network planning by monitoring MQTT broker message rates, topic distribution, and connected client counts to size broker and network infrastructure.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-MQTT-ConnProtoString** | **Generic description:** Donut chart showing the distribution of MQTT ConnProtoString data. **Security use case examples:** Analysis of MQTT connprotostring distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports MQTT protocol monitoring for capacity planning and validates that connprotostring patterns align with expected configurations. | `mqtt.connect.protocol_string` | event_type:mqtt |
| **SN-MQTT-ConnProtoVersion** | **Generic description:** Donut chart showing the distribution of MQTT ConnProtoVersion data. **Security use case examples:** Analysis of MQTT connprotoversion distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports MQTT protocol monitoring for capacity planning and validates that connprotoversion patterns align with expected configurations. | `mqtt.connect.protocol_version` | event_type:mqtt |
| **SN-MQTT-ConnUsernames** | **Generic description:** Donut chart showing the distribution of MQTT ConnUsernames data. **Security use case examples:** Analysis of MQTT connusernames distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports MQTT protocol monitoring for capacity planning and validates that connusernames patterns align with expected configurations. | `mqtt.connect.username` | event_type:mqtt |
| **SN-MQTT-MqttOverTime** | **Generic description:** Bar chart showing MQTT event volume over time. **Security use case examples:** Tracking MQTT traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides MQTT traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | `timestamp` | event_type:mqtt |
| **SN-MQTT-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for MQTT connections. **Security use case examples:** Identifying the most active MQTT destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that MQTT traffic uses only expected destination IPs. | `dest_ip` | event_type:mqtt |
| **SN-MQTT-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for MQTT connections. **Security use case examples:** Identifying the most active MQTT destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that MQTT traffic uses only expected destination ports. | `dest_port` | event_type:mqtt |
| **SN-MQTT-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for MQTT connections. **Security use case examples:** Identifying the most active MQTT source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that MQTT traffic uses only expected source IPs. | `src_ip` | event_type:mqtt |
| **SN-MQTT-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for MQTT connections. **Security use case examples:** Identifying the most active MQTT source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that MQTT traffic uses only expected source ports. | `src_port` | event_type:mqtt |
| **SN-MQTT-Total** | **Generic description:** Single metric display showing the total count of MQTT events. **Security use case examples:** Provides an at-a-glance MQTT event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary MQTT traffic KPI for operational monitoring and capacity planning. | — | event_type:mqtt |
| **SN-MQTT-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-MQTT events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of MQTT events | — |

## Dashboard: SN-NFS

**Security use case examples:** Monitors Network File System (NFS) traffic for detecting unauthorised file access, data exfiltration via NFS, and storage policy violations.

**Network use case examples:** Enables NFS storage infrastructure capacity planning by tracking mount counts, data transfer volumes, and NFS version distribution for storage network sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-NFS-ByDestIP** | **Generic description:** Data table showing the distribution of NFS ByDestIP data. **Security use case examples:** Analysis of NFS destip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that destip patterns align with expected configurations. | `dest_ip` | event_type:nfs |
| **SN-NFS-ByDestPort** | **Generic description:** Data table showing the distribution of NFS ByDestPort data. **Security use case examples:** Analysis of NFS destport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that destport patterns align with expected configurations. | `dest_port` | event_type:nfs |
| **SN-NFS-ByFileName** | **Generic description:** Donut chart showing the distribution of NFS ByFileName data. **Security use case examples:** Analysis of NFS filename distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that filename patterns align with expected configurations. | `nfs.filename` | event_type:nfs |
| **SN-NFS-ByFileTx** | **Generic description:** Donut chart showing the distribution of NFS ByFileTx data. **Security use case examples:** Analysis of NFS filetx distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that filetx patterns align with expected configurations. | `nfs.file_tx` | event_type:nfs |
| **SN-NFS-ByProcedure** | **Generic description:** Donut chart showing the distribution of NFS ByProcedure data. **Security use case examples:** Analysis of NFS procedure distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that procedure patterns align with expected configurations. | `nfs.procedure` | event_type:nfs |
| **SN-NFS-BySrcIP** | **Generic description:** Data table showing the distribution of NFS BySrcIP data. **Security use case examples:** Analysis of NFS srcip distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that srcip patterns align with expected configurations. | `src_ip` | event_type:nfs |
| **SN-NFS-BySrcPort** | **Generic description:** Data table showing the distribution of NFS BySrcPort data. **Security use case examples:** Analysis of NFS srcport distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that srcport patterns align with expected configurations. | `src_port` | event_type:nfs |
| **SN-NFS-ByStatus** | **Generic description:** Donut chart showing the distribution of NFS ByStatus data. **Security use case examples:** Analysis of NFS status distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that status patterns align with expected configurations. | `nfs.status` | event_type:nfs |
| **SN-NFS-ByType** | **Generic description:** Donut chart showing the distribution of NFS ByType data. **Security use case examples:** Analysis of NFS type distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that type patterns align with expected configurations. | `nfs.type` | event_type:nfs |
| **SN-NFS-ByVersion** | **Generic description:** Donut chart showing the distribution of NFS ByVersion data. **Security use case examples:** Analysis of NFS version distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports NFS protocol monitoring for capacity planning and validates that version patterns align with expected configurations. | `nfs.version` | event_type:nfs |
| **SN-NFS-EventsOverTimeByVersion** | **Generic description:** Bar chart showing NFS event volume over time. **Security use case examples:** Tracking NFS traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides NFS traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of NFS events | event_type:nfs |
| **SN-NFS-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of NFS traffic. **Security use case examples:** Visualises geographic origins of NFS connections to detect anomalous or unexpected source countries. **Network use case examples:** Supports geographic access policy validation by confirming NFS traffic flows to expected locations. | `geoip.location` | event_type:nfs |
| **SN-NFS-TotalCount** | **Generic description:** Single metric display showing the total count of NFS events. **Security use case examples:** Provides an at-a-glance NFS event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary NFS traffic KPI for operational monitoring and capacity planning. | — | event_type:nfs |
| **SN-NFS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-NFS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of NFS events | — |

## Dashboard: SN-OVERVIEW

**Security use case examples:** Comprehensive multi-protocol network overview combining Timelion charts for DNS, HTTP, ICMP, TCP, and alert events with raw event searches for operational visibility.

**Network use case examples:** Provides operations teams with a single-pane view of overall network activity across all major protocols for rapid health assessment, capacity trending, and traffic baseline validation.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Timelion-DNS-NULL** | **Generic description:** Timelion time-series chart counting DNS queries and responses with a NULL record type over time. **Security use case examples:** DNS NULL records are rarely used legitimately and are a common covert channel for DNS tunnelling-based data exfiltration or C2 communication. **Network use case examples:** Establishes a zero-baseline for NULL record traffic, making any sudden appearance immediately visible for network policy enforcement. | — | event_type:dns |
| **SN-Timelion-DNS-NXDOMAIN** | **Generic description:** Timelion time-series chart counting DNS NXDOMAIN (non-existent domain) responses over time. **Security use case examples:** High NXDOMAIN rates can indicate domain generation algorithm (DGA) malware attempting to locate active C2 domains, or misconfigured applications generating noise. **Network use case examples:** Tracks NXDOMAIN volume trends to validate DNS resolver health and detect configuration drift causing unnecessary resolution failures. | — | event_type:dns |
| **SN-Timelion-DNS-TXT** | **Generic description:** Timelion time-series chart plotting DNS TXT record query and response counts over time. **Security use case examples:** DNS TXT records are frequently abused for data exfiltration via DNS tunnelling; an unexpected spike warrants investigation of the queried domains and payload sizes. **Network use case examples:** Supports DNS policy monitoring by providing a time-series baseline for TXT record usage, which should remain low in well-managed environments. | — | event_type:dns |
| **SN-Timelion-DNS/request/reply** | **Generic description:** Timelion time-series chart overlaying DNS request counts and DNS reply counts on the same time axis. **Security use case examples:** A growing gap between requests and replies can reveal DNS amplification activity, resolver failures, or query flooding targeting the DNS infrastructure. **Network use case examples:** Provides a fundamental DNS health metric — request/reply ratio — for baseline monitoring and SLA compliance on internal resolver services. | — | event_type:dns |
| **SN-Timelion-HTTP-statuscode-522/523/0** | **Generic description:** Timelion time-series chart tracking HTTP responses with status codes 522, 523, and 0 (connection timeout/unreachable) over time. **Security use case examples:** Elevated rates of these timeout and unreachable codes may indicate CDN or proxy infrastructure failures, scanning probes, or DDoS-induced overload on origin servers. **Network use case examples:** Supports web application availability monitoring by surfacing infrastructure connectivity issues before they escalate into full service outages. | — | event_type:http |
| **SN-Timelion-ICMP-request-reply** | **Generic description:** Timelion time-series chart overlaying ICMP echo request and reply counts on the same time axis. **Security use case examples:** An asymmetric ICMP request-to-reply ratio may indicate ICMP-based tunnelling, covert data exfiltration, or ping flood denial-of-service attempts. **Network use case examples:** Provides a network reachability health metric by tracking ICMP round-trip balance, useful for detecting infrastructure changes or firewall policy inconsistencies. | — | event_type:icmp |
| **SN-Timelion-IPv4/IPv6** | **Generic description:** Timelion time-series chart overlaying IPv4 and IPv6 event counts simultaneously. **Security use case examples:** A sudden shift toward IPv6 traffic in an environment where IPv6 is not expected may indicate tunnelling, evasion of IPv4-only inspection controls, or infrastructure misconfiguration. **Network use case examples:** Tracks the IPv4/IPv6 migration ratio over time, providing data for network modernisation planning and dual-stack deployment monitoring. | — | event_type:all |
| **SN-Timelion-PPS/Alerts** | **Generic description:** Timelion time-series chart overlaying network packets per second (PPS) with IDS alert counts on a shared time axis. **Security use case examples:** Correlating PPS spikes with alert volume helps distinguish genuine attack traffic from Suricata noise or sensor drops under high load conditions. **Network use case examples:** Supports capacity planning by showing the relationship between traffic volume and detection engine load, informing decisions about sensor hardware or tuning thresholds. | — | event_type:all |
| **SN-Timelion-Protocols** | **Generic description:** Timelion time-series chart overlaying event counts for multiple network protocols simultaneously. **Security use case examples:** Sudden shifts in protocol distribution — e.g. spike in raw TCP while DNS drops — can signal protocol tunnelling, C2 channels, or infrastructure changes. **Network use case examples:** Enables network operations teams to track protocol mix trends for capacity planning and to validate that decommissioned protocols no longer appear in traffic. | — | event_type:all |
| **SN-Timelion-S/SA/R** | **Generic description:** Timelion time-series chart tracking TCP SYN, SYN-ACK, and RST packet counts over time. **Security use case examples:** An imbalance between SYN and SYN-ACK counts indicates incomplete handshakes consistent with SYN flood or port scanning; spikes in RST counts may reveal connection rejection storms. **Network use case examples:** Provides a TCP session establishment baseline for health monitoring of network services and detection of misconfigured load balancers or firewalls silently dropping connections. | — | event_type:tcp |
| **SN-Timelion-TCP/UDP-flows** | **Generic description:** Timelion time-series chart plotting TCP and UDP flow counts on the same time axis. **Security use case examples:** Unexpected shifts in the TCP-to-UDP ratio can indicate protocol substitution by malware, UDP-based amplification attacks, or covert tunnelling over DNS/UDP. **Network use case examples:** Enables long-term traffic engineering and capacity planning by tracking the TCP/UDP flow ratio trend across peak and off-peak periods. | — | event_type:flow |
| **SN-SMB-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-OVERVIEW events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SMB events | — |
| **SN-ALERT-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-OVERVIEW events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALERT events | — |
| **SN-DNS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-OVERVIEW events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of DNS events | — |
| **SN-HTTP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-OVERVIEW events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of HTTP events | — |

## Dashboard: SN-RDP

**Security use case examples:** Monitors Remote Desktop Protocol (RDP) traffic to detect brute-force attacks, lateral movement, and policy-violating remote access sessions.

**Network use case examples:** Supports remote access capacity planning by tracking RDP session counts, active endpoint cardinality, and connection duration trends for gateway and broker infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-RDP-Channels** | **Generic description:** Donut chart showing the distribution of RDP Channels data. **Security use case examples:** Analysis of RDP channels distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that channels patterns align with expected configurations. | `rdp.channels` | event_type:rdp |
| **SN-RDP-ClientBuild** | **Generic description:** Donut chart showing the distribution of RDP ClientBuild data. **Security use case examples:** Analysis of RDP clientbuild distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientbuild patterns align with expected configurations. | `rdp.client.build` | event_type:rdp |
| **SN-RDP-ClientCookie** | **Generic description:** Donut chart showing the distribution of RDP ClientCookie data. **Security use case examples:** Analysis of RDP clientcookie distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientcookie patterns align with expected configurations. | `rdp.cookie` | event_type:rdp |
| **SN-RDP-ClientKeyboardType** | **Generic description:** Donut chart showing the distribution of RDP ClientKeyboardType data. **Security use case examples:** Analysis of RDP clientkeyboardtype distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientkeyboardtype patterns align with expected configurations. | `rdp.client.keyboard_layout` | event_type:rdp |
| **SN-RDP-ClientName** | **Generic description:** Donut chart showing the distribution of RDP ClientName data. **Security use case examples:** Analysis of RDP clientname distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientname patterns align with expected configurations. | `rdp.client.client_name` | event_type:rdp |
| **SN-RDP-ClientVersion** | **Generic description:** Donut chart showing the distribution of RDP ClientVersion data. **Security use case examples:** Analysis of RDP clientversion distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that clientversion patterns align with expected configurations. | `rdp.client.version` | event_type:rdp |
| **SN-RDP-Event_Type** | **Generic description:** Donut chart showing the distribution of RDP Event_Type data. **Security use case examples:** Analysis of RDP event_type distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RDP protocol monitoring for capacity planning and validates that event_type patterns align with expected configurations. | `rdp.event_type` | event_type:rdp |
| **SN-RDP-EventsOverTime** | **Generic description:** Bar chart showing RDP event volume over time. **Security use case examples:** Tracking RDP traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides RDP traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of RDP events | event_type:rdp |
| **SN-RDP-Top100-DestIP** | **Generic description:** Data table ranking the top destination IPs for RDP connections. **Security use case examples:** Identifying the most active RDP destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RDP traffic uses only expected destination IPs. | `dest_ip` | event_type:rdp |
| **SN-RDP-Top100-DestPort** | **Generic description:** Data table ranking the top destination ports for RDP connections. **Security use case examples:** Identifying the most active RDP destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RDP traffic uses only expected destination ports. | `dest_port` | event_type:rdp |
| **SN-RDP-Top100-SrcIP** | **Generic description:** Data table ranking the top source IPs for RDP connections. **Security use case examples:** Identifying the most active RDP source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RDP traffic uses only expected source IPs. | `src_ip` | event_type:rdp |
| **SN-RDP-Top100-SrcPort** | **Generic description:** Data table ranking the top source ports for RDP connections. **Security use case examples:** Identifying the most active RDP source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RDP traffic uses only expected source ports. | `src_port` | event_type:rdp |
| **SN-RDP-TotalEvents** | **Generic description:** Single metric display showing the total count of RDP events. **Security use case examples:** Provides an at-a-glance RDP event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary RDP traffic KPI for operational monitoring and capacity planning. | — | event_type:rdp |
| **SN-RDP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-RDP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of RDP events | — |

## Dashboard: SN-RFB

**Security use case examples:** Monitors RFB (VNC) remote desktop protocol traffic to detect unauthorised remote access, brute-force attacks, and unauthenticated VNC sessions.

**Network use case examples:** Enables remote support and management teams to baseline VNC session volumes and monitor remote access infrastructure utilisation for capacity planning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-RFB-Authentication-Sectype** | **Generic description:** Donut chart showing the distribution of RFB Authentication-Sectype data. **Security use case examples:** Analysis of RFB authentication-sectype distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RFB protocol monitoring for capacity planning and validates that authentication-sectype patterns align with expected configurations. | `rfb.authentication.security_type` | event_type:rfb |
| **SN-RFB-ByVlan** | **Generic description:** Donut chart showing the distribution of RFB ByVlan data. **Security use case examples:** Analysis of RFB vlan distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RFB protocol monitoring for capacity planning and validates that vlan patterns align with expected configurations. | `vlan` | event_type:rfb |
| **SN-RFB-EventsOverTime** | **Generic description:** Bar chart showing RFB event volume over time. **Security use case examples:** Tracking RFB traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides RFB traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of RFB events | event_type:rfb |
| **SN-RFB-ScreenShared** | **Generic description:** Donut chart showing the distribution of RFB ScreenShared data. **Security use case examples:** Analysis of RFB screenshared distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RFB protocol monitoring for capacity planning and validates that screenshared patterns align with expected configurations. | `rfb.screen_shared` | event_type:rfb |
| **SN-RFB-Server-Security-Failure** | **Generic description:** Donut chart showing the distribution of RFB Server-Security-Failure data. **Security use case examples:** Analysis of RFB server-security-failure distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports RFB protocol monitoring for capacity planning and validates that server-security-failure patterns align with expected configurations. | `rfb.server_security_failure_reason` | event_type:rfb |
| **SN-RFB-Top100-DestIP** | **Generic description:** Data table ranking the top destination IPs for RFB connections. **Security use case examples:** Identifying the most active RFB destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RFB traffic uses only expected destination IPs. | `dest_ip` | event_type:rfb |
| **SN-RFB-Top100-DestPort** | **Generic description:** Data table ranking the top destination ports for RFB connections. **Security use case examples:** Identifying the most active RFB destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RFB traffic uses only expected destination ports. | `dest_port` | event_type:rfb |
| **SN-RFB-Top100-SrcIP** | **Generic description:** Data table ranking the top source IPs for RFB connections. **Security use case examples:** Identifying the most active RFB source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RFB traffic uses only expected source IPs. | `src_ip` | event_type:rfb |
| **SN-RFB-Top100-SrcPort** | **Generic description:** Data table ranking the top source ports for RFB connections. **Security use case examples:** Identifying the most active RFB source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that RFB traffic uses only expected source ports. | `src_port` | event_type:rfb |
| **SN-RFB-TotalEvents** | **Generic description:** Single metric display showing the total count of RFB events. **Security use case examples:** Provides an at-a-glance RFB event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary RFB traffic KPI for operational monitoring and capacity planning. | — | event_type:rfb |
| **SN-RFB-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-RFB events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of RFB events | — |

## Dashboard: SN-SANS-MTA-Training

**Security use case examples:** Training-focused dashboard combining alert timeline analysis, flow correlation, and Kerberos account login monitoring for security operations course exercises.

**Network use case examples:** Provides a structured multi-protocol dataset view useful for training analysts in correlating flow, alert, and authentication event data for operational network monitoring.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-ALERT-Timeline-msg** | **Generic description:** Data table listing alert messages with their timeline distribution, showing which alert signature messages appear most frequently over time. **Security use case examples:** Timeline-based alert message analysis reveals attack sequence patterns and persistence indicators, enabling analysts to correlate alert messages with attack kill chain phases. **Network use case examples:** Supports security baseline monitoring by tracking which alert messages are consistently present versus newly appearing, aiding distinction between persistent threats and transient noise. | — | event_type:alert |
| **SN-ALERT-Timeline-msg-flowid** | **Generic description:** Data table correlating alert messages with their associated flow IDs, enabling alert-to-connection session mapping. **Security use case examples:** Mapping alert messages to flow IDs allows analysts to pivot from a specific alert to the full session context, accelerating root-cause analysis and scope determination. **Network use case examples:** Supports forensic investigation workflows by linking alerts to network flows, enabling reconstruction of the full communication session associated with each detection. | `timestamp`, `alert.signature`, `alert.target.ip`, `flow_id` | event_type:alert |
| **SN-KRB5-AccountLogins** | **Generic description:** Data table showing Kerberos account login events, ranking accounts by authentication frequency. **Security use case examples:** Accounts with unusually high login rates — particularly service accounts or privileged accounts — may indicate credential abuse, password spraying, or automated account enumeration. **Network use case examples:** Provides account login frequency data for Active Directory access auditing and supports baseline establishment for normal per-account authentication volumes. | — | event_type:krb5 |
| **SN-ALL-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SANS-MTA-Training events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of ALL events | — |

## Dashboard: SN-SIP

**Security use case examples:** Monitors SIP VoIP protocol traffic to detect toll fraud, registration attacks, SIP brute-force, and unauthorised VoIP communications.

**Network use case examples:** Supports VoIP infrastructure planning by monitoring SIP session rates, call volumes, and trunk utilisation for capacity management and service quality baselining.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-SIP-EventsOverTime** | **Generic description:** Bar chart showing SIP event volume over time. **Security use case examples:** Tracking SIP traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides SIP traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of SIP events | event_type:sip |
| **SN-SIP-SipCode** | **Generic description:** Donut chart showing the distribution of SIP SipCode data. **Security use case examples:** Analysis of SIP sipcode distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SIP protocol monitoring for capacity planning and validates that sipcode patterns align with expected configurations. | `sip.code` | event_type:sip |
| **SN-SIP-SipMethod** | **Generic description:** Donut chart showing the distribution of SIP SipMethod data. **Security use case examples:** Analysis of SIP sipmethod distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SIP protocol monitoring for capacity planning and validates that sipmethod patterns align with expected configurations. | `sip.method` | event_type:sip |
| **SN-SIP-SipReason** | **Generic description:** Donut chart showing the distribution of SIP SipReason data. **Security use case examples:** Analysis of SIP sipreason distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SIP protocol monitoring for capacity planning and validates that sipreason patterns align with expected configurations. | `sip.reason` | event_type:sip |
| **SN-SIP-SipUri** | **Generic description:** Donut chart showing the distribution of SIP SipUri data. **Security use case examples:** Analysis of SIP sipuri distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SIP protocol monitoring for capacity planning and validates that sipuri patterns align with expected configurations. | `sip.uri` | event_type:sip |
| **SN-SIP-SipVersion** | **Generic description:** Donut chart showing the distribution of SIP SipVersion data. **Security use case examples:** Analysis of SIP sipversion distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SIP protocol monitoring for capacity planning and validates that sipversion patterns align with expected configurations. | `sip.version` | event_type:sip |
| **SN-SIP-Top100-DestIP** | **Generic description:** Data table ranking the top destination IPs for SIP connections. **Security use case examples:** Identifying the most active SIP destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SIP traffic uses only expected destination IPs. | `dest_ip` | event_type:sip |
| **SN-SIP-Top100-DestPort** | **Generic description:** Data table ranking the top destination ports for SIP connections. **Security use case examples:** Identifying the most active SIP destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SIP traffic uses only expected destination ports. | `dest_port` | event_type:sip |
| **SN-SIP-Top100-SrcIP** | **Generic description:** Data table ranking the top source IPs for SIP connections. **Security use case examples:** Identifying the most active SIP source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SIP traffic uses only expected source IPs. | `src_ip` | event_type:sip |
| **SN-SIP-Top100-SrcPort** | **Generic description:** Data table ranking the top source ports for SIP connections. **Security use case examples:** Identifying the most active SIP source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SIP traffic uses only expected source ports. | `src_port` | event_type:sip |
| **SN-SIP-TotalEvents** | **Generic description:** Single metric display showing the total count of SIP events. **Security use case examples:** Provides an at-a-glance SIP event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary SIP traffic KPI for operational monitoring and capacity planning. | — | event_type:sip |
| **SN-SIP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SIP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SIP events | — |

## Dashboard: SN-SMB

**Security use case examples:** Provides comprehensive visibility into SMB protocol activity across the network, enabling detection of lateral movement, ransomware propagation, and credential-based file share attacks.

**Network use case examples:** Enables file server and share capacity planning by monitoring SMB session volumes, active client counts, and data transfer rates for storage network sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-SMB-ClientDialect** | **Generic description:** Donut chart showing the distribution of SMB ClientDialect data. **Security use case examples:** Analysis of SMB clientdialect distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that clientdialect patterns align with expected configurations. | `smb.client_dialects` | event_type:smb |
| **SN-SMB-Dialect** | **Generic description:** Donut chart showing the distribution of SMB Dialect data. **Security use case examples:** Analysis of SMB dialect distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that dialect patterns align with expected configurations. | `smb.dialect` | event_type:smb |
| **SN-SMB-EventsOverTime** | **Generic description:** Bar chart showing SMB event volume over time. **Security use case examples:** Tracking SMB traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides SMB traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of SMB events | event_type:smb |
| **SN-SMB-Filename** | **Generic description:** Data table showing the distribution of SMB Filename data. **Security use case examples:** Analysis of SMB filename distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that filename patterns align with expected configurations. | `smb.filename` | event_type:smb |
| **SN-SMB-Function** | **Generic description:** Donut chart showing the distribution of SMB Function data. **Security use case examples:** Analysis of SMB function distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that function patterns align with expected configurations. | `smb.function` | event_type:smb |
| **SN-SMB-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of SMB traffic. **Security use case examples:** Visualises geographic origins of SMB connections to detect anomalous or unexpected source countries. **Network use case examples:** Supports geographic access policy validation by confirming SMB traffic flows to expected locations. | `geoip.location` | event_type:smb |
| **SN-SMB-KerberosSnames** | **Generic description:** Donut chart showing the distribution of SMB KerberosSnames data. **Security use case examples:** Analysis of SMB kerberossnames distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that kerberossnames patterns align with expected configurations. | `smb.kerberos.snames` | event_type:smb |
| **SN-SMB-NtlmsspDomain** | **Generic description:** Donut chart showing the distribution of SMB NtlmsspDomain data. **Security use case examples:** Analysis of SMB ntlmsspdomain distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that ntlmsspdomain patterns align with expected configurations. | `smb.ntlmssp.domain` | event_type:smb |
| **SN-SMB-NtlmsspHost** | **Generic description:** Donut chart showing the distribution of SMB NtlmsspHost data. **Security use case examples:** Analysis of SMB ntlmssphost distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that ntlmssphost patterns align with expected configurations. | `smb.ntlmssp.host` | event_type:smb |
| **SN-SMB-NtlmsspUser** | **Generic description:** Donut chart showing the distribution of SMB NtlmsspUser data. **Security use case examples:** Analysis of SMB ntlmsspuser distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that ntlmsspuser patterns align with expected configurations. | `smb.ntlmssp.user` | event_type:smb |
| **SN-SMB-ServerGUID** | **Generic description:** Donut chart showing the distribution of SMB ServerGUID data. **Security use case examples:** Analysis of SMB serverguid distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that serverguid patterns align with expected configurations. | `smb.server_guid` | event_type:smb |
| **SN-SMB-Share** | **Generic description:** Data table showing the distribution of SMB Share data. **Security use case examples:** Analysis of SMB share distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that share patterns align with expected configurations. | `smb.share` | event_type:smb |
| **SN-SMB-Status** | **Generic description:** Donut chart showing the distribution of SMB Status data. **Security use case examples:** Analysis of SMB status distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SMB protocol monitoring for capacity planning and validates that status patterns align with expected configurations. | `smb.status` | event_type:smb |
| **SN-SMB-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for SMB connections. **Security use case examples:** Identifying the most active SMB destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SMB traffic uses only expected destination IPs. | `dest_ip` | event_type:smb |
| **SN-SMB-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for SMB connections. **Security use case examples:** Identifying the most active SMB destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SMB traffic uses only expected destination ports. | `dest_port` | event_type:smb |
| **SN-SMB-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for SMB connections. **Security use case examples:** Identifying the most active SMB source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SMB traffic uses only expected source IPs. | `src_ip` | event_type:smb |
| **SN-SMB-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for SMB connections. **Security use case examples:** Identifying the most active SMB source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SMB traffic uses only expected source ports. | `src_port` | event_type:smb |
| **SN-SMB-Total** | **Generic description:** Single metric display showing the total count of SMB events. **Security use case examples:** Provides an at-a-glance SMB event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary SMB traffic KPI for operational monitoring and capacity planning. | — | event_type:smb |
| **SN-SMB-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SMB events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SMB events | — |

## Dashboard: SN-SMTP

**Security use case examples:** Monitors email (SMTP) protocol traffic to detect phishing campaigns, malware email delivery, spam relay abuse, and business email compromise activity.

**Network use case examples:** Supports mail infrastructure capacity planning by tracking SMTP message volumes, relay session counts, and sender-to-recipient distribution for MTA sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-SMTP-AttachmentsExtension** | **Generic description:** Line chart showing email attachment file extension distribution over time in SMTP traffic. **Security use case examples:** Spikes in executable attachment extensions (.exe, .js, .bat, .vbs) are strong indicators of malware distribution campaigns or phishing waves. **Network use case examples:** Provides an email attachment content baseline for DLP policy validation and email security gateway rule development. | `timestamp` | event_type:smtp |
| **SN-SMTP-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of SMTP traffic source and destination IPs. **Security use case examples:** Geographic anomalies in SMTP traffic — unexpected sending countries or unusual relay paths — may indicate spam campaigns, compromised mail relay abuse, or phishing infrastructure. **Network use case examples:** Supports mail relay policy and international email routing decisions by providing a geographic view of SMTP connection origins and destinations. | `geoip.location` | event_type:smtp |
| **SN-SMTP-SmtpOverTime** | **Generic description:** Line chart showing SMTP event volume over time. **Security use case examples:** SMTP volume spikes outside business hours, or from unexpected source ranges, may indicate compromised email accounts being used for spam, phishing campaigns, or mail bombing. **Network use case examples:** Provides an email traffic capacity baseline for mail server sizing and detecting seasonal volume trends for infrastructure planning. | `timestamp` | event_type:smtp |
| **SN-SMTP-Top20DestIP** | **Generic description:** Data table ranking the top 20 destination IPs receiving SMTP connections. **Security use case examples:** Unexpected or non-standard mail server IPs appearing at the top may indicate mail relay misconfiguration, spam relay abuse, or connections to known malicious mail infrastructure. **Network use case examples:** Maps mail server topology by showing SMTP connection targets, supporting mail routing audits and MX record verification. | `dest_ip` | event_type:smtp |
| **SN-SMTP-Top20DestPort** | **Generic description:** Data table ranking the top 20 destination ports used in SMTP connections. **Security use case examples:** SMTP on non-standard ports (other than 25, 465, 587) may indicate mail relays bypassing gateway inspection or malware using custom mail ports to evade policy. **Network use case examples:** Validates mail gateway port configuration by confirming SMTP traffic uses only approved destination ports. | `dest_port` | event_type:smtp |
| **SN-SMTP-Top20mail_from** | **Generic description:** Bar chart showing the top 20 MAIL FROM sender addresses in SMTP traffic. **Security use case examples:** High-frequency sender addresses sending to many recipients may indicate compromised accounts used for spam or phishing campaigns, or mailing lists generating bulk traffic. **Network use case examples:** Provides sender address inventory for email DLP and data governance, tracking which internal accounts generate the highest outbound email volumes. | `smtp.mail_from` | event_type:smtp |
| **SN-SMTP-Top20MailApplications** | **Generic description:** Donut chart showing the top 20 mail client applications identified in SMTP traffic. **Security use case examples:** Unknown or unexpected mail client applications may indicate compromised mail clients, spam tools, or policy-violating mail software in the environment. **Network use case examples:** Provides a mail client software inventory for IT asset management and validates that only approved mail applications are generating SMTP traffic. | `email.x_mailer` | event_type:smtp |
| **SN-SMTP-Top20MailOrganisations** | **Generic description:** Donut chart showing the top 20 sending organisations identified from SMTP traffic metadata. **Security use case examples:** Unexpected or unknown organisations appearing as top mail senders may indicate phishing campaigns, business email compromise (BEC), or misconfigured mail authentication. **Network use case examples:** Supports vendor and partner email monitoring by tracking which external organisations generate the most inbound SMTP traffic. | `email.organization` | event_type:smtp |
| **SN-SMTP-Top20MailSendingIPs** | **Generic description:** Donut chart showing the top 20 source IPs sending SMTP/email traffic. **Security use case examples:** High volumes from unexpected IP addresses may indicate compromised mail servers, spammer infrastructure, or hosts with misconfigured local mail agents generating noise. **Network use case examples:** Maps the source IP distribution of email traffic for mail relay auditing, supporting SPF policy validation and sender IP reputation management. | `email.x_originating_ip` | event_type:smtp |
| **SN-SMTP-Top20rcpt_to** | **Generic description:** Bar chart showing the top 20 RCPT TO recipient addresses in SMTP traffic. **Security use case examples:** Repeated delivery attempts to non-existent recipients may indicate harvesting or enumeration of valid email addresses in the domain. **Network use case examples:** Supports mail server capacity planning by identifying the highest-volume recipients and validating that distribution lists are within expected size bounds. | `smtp.rcpt_to` | event_type:smtp |
| **SN-SMTP-Top20SrcIP** | **Generic description:** Data table ranking the top 20 source IPs initiating SMTP connections. **Security use case examples:** Unexpected internal or external source IPs initiating SMTP connections may indicate compromised hosts running unauthorised mail agents or spam relay activity. **Network use case examples:** Provides source IP data for mail relay topology documentation and SMTP access policy enforcement. | `src_ip` | event_type:smtp |
| **SN-SMTP-Top20SrcPort** | **Generic description:** Data table ranking the top 20 source ports used in SMTP traffic. **Security use case examples:** Non-ephemeral or reused source ports in SMTP connections may indicate low-volume persistent mail relay connections or custom mail tooling with fixed port behaviour. **Network use case examples:** Validates SMTP connection behaviour patterns for mail relay auditing and network firewall policy review. | `src_port` | event_type:smtp |
| **SN-SMTP-Total** | **Generic description:** Single large-number metric showing the total count of SMTP events in the selected time range. **Security use case examples:** Provides an at-a-glance email traffic volume indicator for detecting unusual surges during off-hours that may indicate spam campaigns or mail server abuse. **Network use case examples:** Serves as a primary email infrastructure KPI for capacity planning and operational monitoring dashboards. | — | event_type:smtp |
| **SN-SMTP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SMTP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SMTP events | — |

## Dashboard: SN-SNMP

**Security use case examples:** Monitors SNMP protocol activity across network infrastructure, detecting unauthorised polling, weak community strings, and network device enumeration attempts.

**Network use case examples:** Enables network management infrastructure planning by monitoring SNMP polling volumes, managed device counts, and community access patterns for management plane capacity.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-SNMP-ByVlan** | **Generic description:** Donut chart showing the distribution of SNMP ByVlan data. **Security use case examples:** Analysis of SNMP vlan distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that vlan patterns align with expected configurations. | `vlan` | event_type:snmp |
| **SN-SNMP-Community** | **Generic description:** Data table showing the distribution of SNMP Community data. **Security use case examples:** Analysis of SNMP community distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that community patterns align with expected configurations. | `snmp.community` | event_type:snmp |
| **SN-SNMP-EventsOverTime** | **Generic description:** Bar chart showing SNMP event volume over time. **Security use case examples:** Tracking SNMP traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides SNMP traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of SNMP events | event_type:snmp |
| **SN-SNMP-Pdu** | **Generic description:** Donut chart showing the distribution of SNMP Pdu data. **Security use case examples:** Analysis of SNMP pdu distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that pdu patterns align with expected configurations. | `snmp.pdu_type` | event_type:snmp |
| **SN-SNMP-Top100-DestIP** | **Generic description:** Data table ranking the top destination IPs for SNMP connections. **Security use case examples:** Identifying the most active SNMP destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SNMP traffic uses only expected destination IPs. | `dest_ip` | event_type:snmp |
| **SN-SNMP-Top100-DestPort** | **Generic description:** Data table ranking the top destination ports for SNMP connections. **Security use case examples:** Identifying the most active SNMP destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SNMP traffic uses only expected destination ports. | `dest_port` | event_type:snmp |
| **SN-SNMP-Top100-SrcIP** | **Generic description:** Data table ranking the top source IPs for SNMP connections. **Security use case examples:** Identifying the most active SNMP source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SNMP traffic uses only expected source IPs. | `src_ip` | event_type:snmp |
| **SN-SNMP-Top100-SrcPort** | **Generic description:** Data table ranking the top source ports for SNMP connections. **Security use case examples:** Identifying the most active SNMP source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that SNMP traffic uses only expected source ports. | `src_port` | event_type:snmp |
| **SN-SNMP-TotalCount** | **Generic description:** Single metric display showing the total count of SNMP events. **Security use case examples:** Provides an at-a-glance SNMP event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary SNMP traffic KPI for operational monitoring and capacity planning. | — | event_type:snmp |
| **SN-SNMP-Usm** | **Generic description:** Donut chart showing the distribution of SNMP Usm data. **Security use case examples:** Analysis of SNMP usm distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that usm patterns align with expected configurations. | `snmp.usm` | event_type:snmp |
| **SN-SNMP-Vars** | **Generic description:** Data table showing the distribution of SNMP Vars data. **Security use case examples:** Analysis of SNMP vars distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that vars patterns align with expected configurations. | `snmp.vars` | event_type:snmp |
| **SN-SNMP-Version** | **Generic description:** Donut chart showing the distribution of SNMP Version data. **Security use case examples:** Analysis of SNMP version distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports SNMP protocol monitoring for capacity planning and validates that version patterns align with expected configurations. | `snmp.version` | event_type:snmp |
| **SN-SNMP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SNMP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SNMP events | — |

## Dashboard: SN-SSH

**Security use case examples:** Provides SSH protocol monitoring for brute-force attack detection, lateral movement identification, and SSH client/server version compliance.

**Network use case examples:** Supports remote access infrastructure capacity planning by tracking SSH session volumes, active endpoint cardinality, and connection duration baselines for gateway sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-SSH-ByClientHashByClientIPByPort** | **Generic description:** Hierarchical data table showing SSH client key hashes grouped by client IP and port, tracking which client keys are used from which hosts. **Security use case examples:** Unexpected client key hashes from known IP addresses may indicate key compromise, credential sharing, or unauthorised key deployment on client endpoints. **Network use case examples:** Supports SSH client key lifecycle management and validates that key distribution policies are being followed across all managed endpoints. | `ssh.client.hassh.hash`, `src_ip`, `ssh.client.software_version`, `src_port`, `ether.src_mac` | event_type:ssh |
| **SN-SSH-ByClientProtoVer** | **Generic description:** Donut chart showing the distribution of SSH client protocol versions (SSH-1, SSH-2) across SSH connections. **Security use case examples:** SSH-1 usage is a critical security finding as the protocol has known vulnerabilities; any SSH-1 connections should trigger immediate remediation of the offending client. **Network use case examples:** Tracks SSH protocol version adoption across the network for compliance reporting and supports migration timelines to enforce SSH-2-only policies. | `ssh.client.proto_version` | event_type:ssh |
| **SN-SSH-ByClientSoftwareVer** | **Generic description:** Donut chart showing the distribution of SSH client software identifiers (client version strings) observed in SSH handshakes. **Security use case examples:** Unknown or unusual client software strings may indicate custom tools, automated scanners, or malware SSH clients attempting to blend in with legitimate traffic. **Network use case examples:** Provides an SSH client software inventory for patch management and validates that only approved SSH client software versions are in use across the network. | `ssh.client.software_version` | event_type:ssh |
| **SN-SSH-ByServerHashByServerIPByPort** | **Generic description:** Hierarchical data table showing SSH server host key hashes grouped by server IP and port, enabling host key inventory and change detection. **Security use case examples:** Host key changes detected via this table may indicate SSH server replacement, MitM interception, or key rotation events that require validation and certificate management actions. **Network use case examples:** Enables SSH host key inventory management across the network infrastructure, supporting compliance requirements for SSH key rotation and server authentication auditing. | `ssh.server.hassh.hash`, `dest_ip`, `ssh.server.software_version`, `dest_port`, `ether.dest_mac` | event_type:ssh |
| **SN-SSH-ByServerProtoVer** | **Generic description:** Donut chart showing the distribution of SSH server protocol versions observed across SSH connections. **Security use case examples:** SSH servers still advertising SSH-1 compatibility are misconfigured; this view enables targeted remediation of servers with deprecated protocol support enabled. **Network use case examples:** Supports SSH server compliance auditing by inventorying server protocol version capabilities and tracking progress toward SSH-2-only enforcement. | `ssh.server.proto_version` | event_type:ssh |
| **SN-SSH-ByServerSoftwareVer** | **Generic description:** Donut chart showing the distribution of SSH server software version strings from SSH banners. **Security use case examples:** Outdated SSH server software versions with known CVEs are high-priority patch targets; unexpected server software may indicate unauthorised services. **Network use case examples:** Enables SSH server software inventory for vulnerability management, supporting patch prioritisation based on version exposure across monitored SSH servers. | `ssh.server.software_version` | event_type:ssh |
| **SN-SSH-EventsOverTime** | **Generic description:** Line chart showing SSH connection event volume over time. **Security use case examples:** SSH volume spikes, especially from external IPs or during off-hours, are a strong indicator of brute-force campaigns, credential stuffing, or lateral movement activity. **Network use case examples:** Provides an SSH traffic baseline for capacity planning and validates SSH gateway utilisation patterns for infrastructure sizing decisions. | List of SSH events | event_type:ssh |
| **SN-SSH-GeoIP** | **Generic description:** Geographic heatmap overlaying SSH connection source and destination IPs on a world map by geolocation. **Security use case examples:** Geographic anomalies in SSH traffic — connections from countries where the organisation has no legitimate activity — indicate external brute-force or remote access policy violations. **Network use case examples:** Supports SSH access geographic policy enforcement and validates that VPN and jump-host infrastructure is directing SSH traffic through expected network paths. | `geoip.location` | event_type:ssh |
| **SN-SSH-Top20DestIP** | **Generic description:** Data table ranking the top 20 destination IPs receiving SSH connections. **Security use case examples:** Unexpected destination IPs receiving SSH traffic may indicate lateral movement, compromised internal systems being used as SSH relay hops, or unauthorised SSH services. **Network use case examples:** Maps the SSH server population for asset management and validates that SSH access is only occurring to authorised destination hosts. | `dest_ip` | event_type:ssh |
| **SN-SSH-Top20DestPort** | **Generic description:** Data table ranking the top 20 destination ports for SSH connections. **Security use case examples:** SSH on non-standard ports (other than 22) may indicate servers configured to evade detection or clients attempting to bypass firewall port restrictions. **Network use case examples:** Validates SSH port configuration policy by confirming that SSH traffic uses only approved destination ports and flagging policy deviations. | `dest_port` | event_type:ssh |
| **SN-SSH-Top20SrcIP** | **Generic description:** Data table ranking the top 20 source IPs initiating SSH connections. **Security use case examples:** High-volume SSH source IPs — especially external addresses — may indicate ongoing brute-force attacks, credential scanning, or compromised hosts performing lateral movement. **Network use case examples:** Identifies the most active SSH clients for access auditing and supports SSH gateway capacity planning based on top-talker analysis. | `src_ip` | event_type:ssh |
| **SN-SSH-Top20SrcPort** | **Generic description:** Data table ranking the top 20 source ports used in SSH connections. **Security use case examples:** Source port analysis in SSH traffic helps identify session reuse patterns or fixed source-port tools that may deviate from expected ephemeral port behaviour. **Network use case examples:** Supports SSH session behaviour baseline monitoring and validates that ephemeral source port randomisation is operating correctly in the network. | `src_port` | event_type:ssh |
| **SN-SSH-Total** | **Generic description:** Single large-number metric displaying the total count of SSH events in the selected time range. **Security use case examples:** An SSH event count far above or below baseline is an immediate indicator of potential brute-force waves, SSH infrastructure failures, or sensor coverage gaps. **Network use case examples:** Serves as a primary SSH infrastructure KPI for operational monitoring and capacity planning dashboards. | — | event_type:ssh |
| **SN-SSH-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-SSH events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of SSH events | — |

## Dashboard: SN-STATS

**Security use case examples:** Displays Suricata engine performance statistics including packet capture rates, memory usage, fragmentation, and decoder throughput for sensor health monitoring.

**Network use case examples:** Provides Suricata sensor health dashboards for operations teams to monitor capture performance, decoder throughput, and resource utilisation for sensor capacity planning.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Stats-CapturedPktsVsGaps** | **Generic description:** Dual-axis line chart comparing Suricata captured packet counts against packet gap counts over time. **Security use case examples:** Packet gaps indicate sensor packet drops or NIC buffer overflows; high gap rates compromise IDS coverage integrity and may allow attack traffic to pass unseen. **Network use case examples:** Provides a sensor health indicator for capacity planning, enabling operations teams to identify when sensor hardware or capture configuration needs upgrading. | `stats.capture.kernel_packets`, `timestamp`, `stats.tcp.reassembly_gap` | event_type:all |
| **SN-Stats-DecoderAvgMaxPktSize** | **Generic description:** Line chart showing the average and maximum decoded packet size over time as reported by Suricata's decoder stats. **Security use case examples:** Unusually large maximum packet sizes can indicate jumbo frame issues, packet reconstruction problems, or exploitation of oversized-packet handling vulnerabilities. **Network use case examples:** Supports network MTU and packet sizing policy monitoring, helping validate that network segments conform to expected maximum transmission unit settings. | `stats.decoder.avg_pkt_size`, `timestamp`, `stats.decoder.max_pkt_size` | event_type:all |
| **SN-Stats-DecoderBytes-Packets** | **Generic description:** Single large-number metric showing total bytes and packets processed by Suricata's decoder. **Security use case examples:** Sudden drops in decoded bytes or packets indicate sensor capture failures, NIC issues, or traffic engineering changes that reduce sensor visibility. **Network use case examples:** Provides key decoder throughput KPIs for sensor capacity reporting and validates that traffic volumes are within the sensor's rated processing capacity. | `stats.decoder.bytes`, `stats.decoder.pkts` | event_type:all |
| **SN-Stats-DecoderProto-Deltas** | **Generic description:** Line chart showing per-protocol decoded packet count changes (deltas) over time from Suricata stats. **Security use case examples:** Protocol delta anomalies — unexpected protocol spikes in decoder stats — can confirm whether observed network traffic changes are genuine or artifacts of decoder issues. **Network use case examples:** Supports decoder performance monitoring and validates that all expected protocol decoders are active and processing traffic at expected rates. | `stats.decoder.erspan_delta`, `stats.decoder.ethernet_delta`, `stats.decoder.gre_delta`, `stats.decoder.icmpv4_delta`, `stats.decoder.icmpv6_delta`, `stats.decoder.ipv4_delta`, `stats.decoder.ipv4_in_ipv6_delta`, `stats.decoder.ipv6_delta`, `stats.decoder.ipv6_in_ipv6_delta`, `stats.decoder.mpls_delta`, `stats.decoder.ppp_delta`, `stats.decoder.pppoe_delta`, `stats.decoder.sctp_delta`, `stats.decoder.sll_delta`, `stats.decoder.tcp_delta`, `stats.decoder.udp_delta`, `stats.decoder.vlan_delta`, `stats.decoder.vlan_qinq_delta`, `timestamp` | event_type:all |
| **SN-Stats-EmergencyMode** | **Generic description:** Bar chart showing Suricata emergency mode activations over time. **Security use case examples:** Emergency mode activates when Suricata detects memory pressure or severe packet drops; frequent activations indicate the sensor is under-resourced and coverage is degraded. **Network use case examples:** Provides a critical sensor health indicator for infrastructure capacity planning, triggering hardware upgrade investigations when activations exceed acceptable thresholds. | `stats.flow.emerg_mode_entered`, `timestamp`, `stats.flow.emerg_mode_over` | event_type:all |
| **SN-Stats-Frags** | **Generic description:** Data table showing IPv4/IPv6 fragment reassembly statistics from Suricata. **Security use case examples:** High fragment counts or reassembly failures may indicate fragmentation-based evasion attacks, MTU misconfiguration, or network infrastructure issues generating excessive fragmentation. **Network use case examples:** Supports network MTU policy monitoring and validates that packet fragmentation rates are within expected bounds for the monitored environment. | `stats.defrag.ipv4.fragments`, `stats.defrag.ipv6.fragments`, `stats.defrag.max_frag_hits`, `timestamp` | event_type:all |
| **SN-Stats-Frags-Deltas** | **Generic description:** Line chart showing changes (deltas) in fragment reassembly statistics over time. **Security use case examples:** Sudden spikes in fragment delta rates may indicate targeted fragmentation-based evasion attempts or network topology changes affecting packet reassembly. **Network use case examples:** Tracks fragmentation rate trends for network health monitoring and helps correlate fragmentation anomalies with infrastructure change events. | `stats.defrag.ipv4.fragments_delta`, `timestamp`, `stats.defrag.ipv6.fragments_delta`, `stats.defrag.max_frag_hits_delta` | event_type:all |
| **SN-Stats-ipv4-ipv6-fragments** | **Generic description:** Bar chart showing the count of IPv4 and IPv6 fragmented packets over time. **Security use case examples:** High fragmentation rates can indicate fragmentation-based IDS evasion techniques or network topology misconfigurations generating unnecessary packet fragmentation. **Network use case examples:** Tracks IPv4/IPv6 fragmentation levels for network health monitoring and validates that fragmentation policies are operating correctly across monitored segments. | `stats.defrag.ipv4.fragments`, `timestamp`, `stats.defrag.ipv6.fragments` | event_type:all |
| **SN-Stats-KernelPacketsAndDrops-Deltas** | **Generic description:** Line chart showing deltas in kernel-level captured packets and kernel-level drops over time. **Security use case examples:** Rising kernel drop deltas indicate the capture interface is being overwhelmed; sustained drops compromise IDS coverage and may allow attack traffic to pass undetected. **Network use case examples:** Provides kernel capture performance trending data for sensor capacity planning and upgrade justification reporting. | `stats.capture.kernel_drops_delta`, `stats.capture.kernel_packets_delta`, `timestamp` | event_type:all |
| **SN-Stats-Memcap-Deltas** | **Generic description:** Line chart showing changes in Suricata memory cap hit counts over time. **Security use case examples:** Memcap hits indicate Suricata is reaching memory allocation limits, degrading protocol tracking and detection capabilities; frequent hits require memory expansion or tuning. **Network use case examples:** Supports sensor memory capacity planning by tracking memcap events, informing decisions about Suricata memory tuning and hardware resource allocation. | `stats.dns.memcap_global_delta`, `timestamp`, `stats.dns.memcap_state_delta`, `stats.flow.memcap_delta`, `stats.http.memcap_delta`, `stats.tcp.segment_memcap_drop_delta`, `stats.tcp.ssn_memcap_drop_delta` | event_type:all |
| **SN-Stats-memuse-Deltas** | **Generic description:** Line chart showing Suricata memory usage changes (deltas) over time. **Security use case examples:** Sustained memory usage growth trends indicate potential memory leaks in Suricata or continuously growing state tables that may eventually cause stability issues. **Network use case examples:** Supports capacity planning for sensor memory resources by tracking memory usage trends and identifying when additional memory allocation or tuning is required. | `stats.dns.memuse_delta`, `stats.http.memuse_delta`, `stats.flow.memuse_delta`, `stats.tcp.memuse_delta`, `stats.tcp.reassembly_memuse_delta`, `timestamp` | event_type:all |
| **SN-Stats-TotalKernelPackets** | **Generic description:** Single large-number metric showing the total packets received at the kernel capture interface. **Security use case examples:** Comparison of kernel packets against Suricata processed packets reveals the overall drop rate, indicating sensor coverage quality and packet loss extent. **Network use case examples:** Provides a primary throughput KPI for sensor capacity reporting and validates that capture rates are within the sensor's processing capacity. | `stats.capture.kernel_packets` | event_type:all |
| **SN-STATS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-STATS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of STATS events | — |

## Dashboard: SN-TFTP

**Security use case examples:** Monitors Trivial File Transfer Protocol (TFTP) traffic for detecting unauthorised firmware transfers, configuration file harvesting, and out-of-window network management activity.

**Network use case examples:** Enables network operations to track TFTP transfer volumes and device counts to plan firmware distribution infrastructure and configuration management capacity.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-TFTP-EventsOverTime** | **Generic description:** Bar chart showing TFTP event volume over time. **Security use case examples:** Tracking TFTP traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides TFTP traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of TFTP events | event_type:tftp |
| **SN-TFTP-File** | **Generic description:** Donut chart showing the distribution of TFTP File data. **Security use case examples:** Analysis of TFTP file distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TFTP protocol monitoring for capacity planning and validates that file patterns align with expected configurations. | `tftp.file` | event_type:tftp |
| **SN-TFTP-GeoIP** | **Generic description:** Geographic heatmap showing the geolocation distribution of TFTP traffic. **Security use case examples:** Visualises geographic origins of TFTP connections to detect anomalous or unexpected source countries. **Network use case examples:** Supports geographic access policy validation by confirming TFTP traffic flows to expected locations. | `geoip.location` | event_type:tftp |
| **SN-TFTP-Mode** | **Generic description:** Donut chart showing the distribution of TFTP Mode data. **Security use case examples:** Analysis of TFTP mode distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TFTP protocol monitoring for capacity planning and validates that mode patterns align with expected configurations. | `tftp.mode` | event_type:tftp |
| **SN-TFTP-Packet** | **Generic description:** Donut chart showing the distribution of TFTP Packet data. **Security use case examples:** Analysis of TFTP packet distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TFTP protocol monitoring for capacity planning and validates that packet patterns align with expected configurations. | `tftp.packet` | event_type:tftp |
| **SN-TFTP-Top20DestIP** | **Generic description:** Data table ranking the top destination IPs for TFTP connections. **Security use case examples:** Identifying the most active TFTP destination IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that TFTP traffic uses only expected destination IPs. | `dest_ip` | event_type:tftp |
| **SN-TFTP-Top20DestPort** | **Generic description:** Data table ranking the top destination ports for TFTP connections. **Security use case examples:** Identifying the most active TFTP destination ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that TFTP traffic uses only expected destination ports. | `dest_port` | event_type:tftp |
| **SN-TFTP-Top20SrcIP** | **Generic description:** Data table ranking the top source IPs for TFTP connections. **Security use case examples:** Identifying the most active TFTP source IPs enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that TFTP traffic uses only expected source IPs. | `src_ip` | event_type:tftp |
| **SN-TFTP-Top20SrcPort** | **Generic description:** Data table ranking the top source ports for TFTP connections. **Security use case examples:** Identifying the most active TFTP source ports enables detection of scanning, brute-force, and exfiltration activity. **Network use case examples:** Supports network asset inventory and validates that TFTP traffic uses only expected source ports. | `src_port` | event_type:tftp |
| **SN-TFTP-Total** | **Generic description:** Single metric display showing the total count of TFTP events. **Security use case examples:** Provides an at-a-glance TFTP event count for detecting anomalous surges or drops relative to baseline. **Network use case examples:** Serves as a primary TFTP traffic KPI for operational monitoring and capacity planning. | — | event_type:tftp |
| **SN-TFTP-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-TFTP events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of TFTP events | — |

## Dashboard: SN-TLS

**Security use case examples:** Monitors TLS encrypted traffic metadata including certificate details, JA3/JA3S fingerprints, and TLS version distribution for encrypted threat detection.

**Network use case examples:** Supports TLS infrastructure capacity planning by monitoring certificate expiry timelines, cipher suite adoption rates, and TLS version migration progress across the estate.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-TLS-ByIssuerdn** | **Generic description:** Donut chart showing the distribution of TLS certificate issuer distinguished names (issuerDN) across all TLS connections. **Security use case examples:** An issuerDN appearing only in TLS connections from unusual hosts may indicate self-signed or rogue CA certificates used by malware, C2 servers, or intercepting proxies. **Network use case examples:** Provides a CA trust inventory across the network, supporting PKI policy compliance and detection of unauthorised certificate authorities issuing certificates in the environment. | `tls.issuerdn` | event_type:tls |
| **SN-TLS-ByJa3Hash** | **Generic description:** Ranked data table listing the top JA3 TLS client fingerprint hashes by occurrence count, derived by hashing client-hello parameters including cipher suites, extensions, and elliptic curves. **Security use case examples:** JA3 fingerprints identify the TLS client library regardless of the server being contacted; repeated hashes from unexpected binaries indicate malware, C2 clients, or policy-violating TLS implementations. **Network use case examples:** Enables network-wide TLS client library inventory for compliance and asset management, confirming that only approved client TLS stacks are in use across the environment. | `src_ip`, `tls.ja3.hash` | event_type:tls |
| **SN-TLS-ByJa3SHash** | **Generic description:** Ranked data table listing the top JA3S TLS server-response fingerprint hashes, computed from server-hello parameters including the selected cipher suite and extensions. **Security use case examples:** JA3S hashes fingerprint TLS server behaviour and can expose C2 servers, self-signed certificate infrastructure, or non-standard TLS stacks hiding behind legitimate IP addresses. **Network use case examples:** Supports server-side TLS policy auditing by inventorying server TLS configurations and identifying servers presenting non-standard cipher suite selections. | `dest_ip`, `tls.ja3s.hash`, `tls.sni` | event_type:tls |
| **SN-TLS-BySni** | **Generic description:** Donut chart showing the distribution of TLS SNI (Server Name Indication) values across TLS handshakes. **Security use case examples:** SNI values reveal the intended domain of encrypted TLS connections; unusual domains appearing in SNI data indicate potential C2 communication, phishing site access, or policy violations. **Network use case examples:** Supports outbound TLS policy enforcement by inventorying which domains internal hosts are establishing encrypted connections to, enabling domain-based policy validation. | `tls.sni` | event_type:tls |
| **SN-TLS-BySubject** | **Generic description:** Donut chart showing the distribution of TLS certificate subject distinguished names across observed TLS connections. **Security use case examples:** Unusual or self-signed certificate subjects may indicate adversary-controlled infrastructure using custom certificates for C2, MitM interception, or phishing HTTPS sites. **Network use case examples:** Provides a TLS certificate inventory for PKI compliance monitoring, enabling operations teams to detect unauthorised certificate subjects and validate approved CA usage. | `tls.subject` | event_type:tls |
| **SN-TLS-ByVersion** | **Generic description:** Donut chart showing the distribution of TLS connections by TLS protocol version (TLS 1.0, 1.1, 1.2, 1.3, SSLv3, etc.). **Security use case examples:** Identifies use of deprecated TLS versions (SSLv3, TLS 1.0, TLS 1.1) which are vulnerable to known attacks such as POODLE and BEAST, enabling prioritised remediation of non-compliant services. **Network use case examples:** Tracks TLS version adoption trends over time for compliance reporting (PCI-DSS, NIST) and migration milestones toward TLS 1.2/1.3 enforcement across the network. | `tls.version` | event_type:tls |
| **SN-TLS-EventsOverTime** | **Generic description:** Line chart showing TLS connection event volume over time. **Security use case examples:** TLS volume spikes or drops can indicate TLS inspection bypass attempts, encrypted malware distribution campaigns, or TLS service outages. **Network use case examples:** Provides a TLS traffic volume baseline for capacity planning and monitoring TLS inspection infrastructure utilisation across time periods. | List of TLS events | event_type:tls |
| **SN-TLS-GeoIP** | **Generic description:** Geographic heatmap overlaying TLS connection source and destination IPs on a world map. **Security use case examples:** Encrypted connections to unexpected geographic locations may indicate data exfiltration over TLS, C2 communication to foreign infrastructure, or geo-policy TLS bypass. **Network use case examples:** Supports geo-based TLS policy enforcement by visualising the global distribution of encrypted connection endpoints and validating expected communication patterns. | `geoip.location` | event_type:tls |
| **SN-TLS-Top20DestIP** | **Generic description:** Data table ranking the top 20 destination IPs receiving TLS connections. **Security use case examples:** Unexpected destination IPs receiving high volumes of TLS connections may indicate C2 servers, data exfiltration targets, or malware update infrastructure. **Network use case examples:** Maps TLS server destinations for network topology documentation and validates that encrypted connections are going to approved external endpoints. | `dest_ip` | event_type:tls |
| **SN-TLS-Top20DestPort** | **Generic description:** Data table ranking the top 20 destination ports for TLS connections. **Security use case examples:** TLS on non-standard ports (other than 443, 8443) may indicate covert encrypted channels, non-standard HTTPS services, or TLS tunnelling tools evading inspection. **Network use case examples:** Validates TLS port policy compliance and supports firewall rule development based on actual TLS port usage across the monitored environment. | `dest_port` | event_type:tls |
| **SN-TLS-Top20SrcIP** | **Generic description:** Data table ranking the top 20 source IPs initiating TLS connections. **Security use case examples:** High-volume TLS sources from internal IPs — particularly outside business hours — may indicate malware beaconing, automated data exfiltration, or compromised hosts. **Network use case examples:** Identifies the most active TLS client endpoints for capacity planning and validates approved outbound TLS usage per host. | `src_ip` | event_type:tls |
| **SN-TLS-Top20SrcPort** | **Generic description:** Data table ranking the top 20 source ports used in TLS connections. **Security use case examples:** Unusual source port patterns in TLS traffic may indicate proxy-relayed connections, TLS session reuse tools, or misconfigured client applications. **Network use case examples:** Supports TLS session tracking baseline monitoring and validates that TLS source port behaviour conforms to expected ephemeral port patterns. | `src_port` | event_type:tls |
| **SN-TLS-Total** | **Generic description:** Single large-number metric displaying the total TLS connection count in the selected time range. **Security use case examples:** Provides an at-a-glance TLS traffic volume for detecting anomalous surges (mass scanning, encrypted exfiltration) or drops (TLS service failures, sensor issues). **Network use case examples:** Serves as a key encrypted traffic KPI for TLS inspection infrastructure sizing and operational monitoring reports. | — | event_type:tls |
| **SN-TLS-EventsList (search)** | **Generic description:** Raw event search table displaying the full list of SN-TLS events with all fields. **Security use case examples:** Provides direct access to individual events for incident investigation and forensic analysis. **Network use case examples:** Enables operations teams to validate detection coverage and perform ad-hoc queries against raw event data. | List of TLS events | — |

## Dashboard: SN-TrafficID

**Security use case examples:** Visualises Stamus Networks traffic identification data showing application protocol classification by Traffic ID and label for network visibility and policy enforcement.

**Network use case examples:** Enables network operations teams to audit application protocol usage across the network, validate traffic classification accuracy, and plan bandwidth allocation per application.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-TrafficID-ByTrafficID** | **Generic description:** Donut chart showing the distribution of TRAFFICID ByTrafficID data. **Security use case examples:** Analysis of TRAFFICID trafficid distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TRAFFICID protocol monitoring for capacity planning and validates that trafficid patterns align with expected configurations. | `traffic.id` | event_type:flow |
| **SN-TrafficID-ByTrafficIdOverTime** | **Generic description:** Bar chart showing TRAFFICID event volume over time. **Security use case examples:** Tracking TRAFFICID traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides TRAFFICID traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | `timestamp`, `traffic.id` | event_type:flow |
| **SN-TrafficID-ByTrafficLabel** | **Generic description:** Donut chart showing the distribution of TRAFFICID ByTrafficLabel data. **Security use case examples:** Analysis of TRAFFICID trafficlabel distribution aids in detecting anomalous protocol behaviour and potential attack indicators. **Network use case examples:** Supports TRAFFICID protocol monitoring for capacity planning and validates that trafficlabel patterns align with expected configurations. | `traffic.label` | event_type:flow |
| **SN-TrafficID-ByTrafficLabelOverTime** | **Generic description:** Bar chart showing TRAFFICID event volume over time. **Security use case examples:** Tracking TRAFFICID traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides TRAFFICID traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | `timestamp`, `traffic.label` | event_type:flow |

## Dashboard: SN-VLAN

**Security use case examples:** Provides per-VLAN protocol event monitoring across alerts, DNS, HTTP, SMTP, SSH, TLS, and file transactions to support network segmentation visibility.

**Network use case examples:** Supports network segmentation planning and per-VLAN capacity management by tracking protocol event volumes and traffic distribution across VLANs for infrastructure sizing.

### Visualizations

| Visualization | Description | JSON key | Event Type |
| --- | --- | --- | --- |
| **SN-Alert-ByVLANID** | **Generic description:** Line chart showing IDS alert counts broken down by VLAN ID over time. **Security use case examples:** VLAN-level alert trending identifies which network segments are experiencing elevated threat activity, enabling targeted investigation and segment isolation decisions. **Network use case examples:** Supports network segmentation compliance by verifying that VLAN-based security zones produce expected alert profiles and that cross-VLAN traffic anomalies are detected. | `timestamp`, `vlan` | event_type:alert |
| **SN-Alert-ByVLANIDTop20** | **Generic description:** Donut chart showing the top 20 VLAN IDs by IDS alert volume. **Security use case examples:** Highlights network segments generating disproportionate alert traffic, which may indicate compromised hosts, misconfigured services, or insufficient VLAN-level access controls. **Network use case examples:** Enables VLAN-level capacity and risk reporting, supporting network architecture reviews and decisions about VLAN consolidation or additional monitoring investment. | `vlan` | event_type:alert |
| **SN-PerVLAN-ALERTEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-DNSEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-FILETransEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-HTTPEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-SMTPEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-SSHEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-PerVLAN-TLSEventsOverTime** | **Generic description:** Line chart showing PERVLAN event volume over time. **Security use case examples:** Tracking PERVLAN traffic trends over time enables detection of unusual volume spikes or drops that may indicate attacks or service failures. **Network use case examples:** Provides PERVLAN traffic baseline data for capacity planning and validates protocol volume trends against expected business activity patterns. | List of ALL events | event_type:all |
| **SN-SMTP-Top20VLAN** | **Generic description:** Bar chart showing SMTP event counts broken down by top 20 VLAN IDs. **Security use case examples:** Unexpected VLANs generating high SMTP traffic may indicate VLAN misconfiguration, unintended mail relay paths, or compromised hosts in restricted network segments. **Network use case examples:** Supports VLAN-level mail traffic monitoring for network segmentation compliance and SMTP access policy enforcement. | `vlan` | event_type:smtp |
| **SN-SMTP-Top20VLANsOverTime** | **Generic description:** Line chart showing SMTP event counts per VLAN over time. **Security use case examples:** Sustained SMTP traffic spikes from specific VLANs during off-hours may indicate segmented compromises or mail infrastructure misconfigurations within those segments. **Network use case examples:** Provides VLAN-level email traffic trending data for network capacity planning and per-segment mail policy reviews. | `timestamp`, `vlan` | event_type:smtp |

